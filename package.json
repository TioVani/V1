{
  "name": "qi-luo-shan-he",
  "version": "1.0.0",
  "description": "",
  "main": "game.js",
  "scripts": {
    "convert": "node scripts/convert.js && node scripts/generate_collision_data.js",
    "convert:xls": "node scripts/convert.js --to-xls",
    "build": "npm run convert && rollup -c",
    "build:only": "rollup -c",
    "build:prod": "cross-env NODE_ENV=production rollup -c",
    "watch": "rollup -c -w",
    "dev": "node scripts/ui-sync-server.js & rollup -c -w",
    "test": "node --test tests/**/*.test.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "@rollup/plugin-terser": "^1.0.0",
    "cross-env": "^10.1.0",
    "playwright": "^1.60.0",
    "puppeteer": "^25.0.4",
    "puppeteer-core": "^25.0.2",
    "rollup": "^4.60.2",
    "sharp": "^0.34.5",
    "xlsx": "^0.18.5"
  },
  "dependencies": {
    "ffmpeg-static": "^5.3.0",
    "iconv-lite": "^0.7.2"
  }
}
