# 猫灵对话系统 — 两段式交互优化方案

## 问题

当前方案中，猫灵台词和玩家选项按钮**同时显示**在同一屏幕上。需求是交互分两步：

1. **先选择环节**：只显示玩家可选的话（"你好~" / "喵喵喵？"），没有猫灵台词
2. **再回应环节**：猫灵根据选择给出回应台词

即每次来访的交互是：

```
玩家接近猫灵 → 按下交互键
  → 出现两个选项: "你好~"  "喵喵喵？"
  → 玩家点击其中一个
  → 猫灵回应台词（显示在对话框中）
  → 对话结束
```

---

## 二、数据结构变化

### 对话脚本格式（NpcDialogueConfig.js）

原格式：
```
一行 = { text: "猫灵台词", choices: [...] }   ← 台词和选项同时出现
```

新格式：拆为两行
```
第一行 = { choices: [...] }           ← 纯选项行（无 text，或 text 为提示语）
第二行 = { text: "猫灵回应" }          ← 纯台词行（无 choices）
```

完整脚本数组则是交错结构：

```
[
    { choices: [...] },      // 玩家选择
    { text: "猫灵回应" },    // 猫灵回应
    { choices: [...] },      // 下一轮选择（如果有）
    { text: "猫灵回应" },    // ...
]
```

### 猫灵具体对话脚本

```javascript
getScript: function (state) {
    var v = state.visitCount;

    // 第3次已结束，后续访问 → 只有台词，无选项
    if (state.resolved) {
        if (state.gaveReward) {
            return [{ text: '喵~（猫灵冲你眨了眨眼）' }];
        }
        return [{ text: '……' }];
    }

    // 第1次：先选 → 再回应
    if (v === 0) {
        return [
            { choices: [
                { label: '你好~', key: 'hello' },
                { label: '喵喵喵？', key: 'meow' }
            ]},
            { text: '喵。' }
        ];
    }

    // 第2次：先选 → 再回应
    if (v === 1) {
        return [
            { choices: [
                { label: '你好~', key: 'hello' },
                { label: '喵喵喵？', key: 'meow' }
            ]},
            { text: '喵喵喵？' }
        ];
    }

    // 第3次：先选 → 再回应（台词根据前两次选择变化）
    var c1 = state.choice1;
    var c2 = state.choice2;
    var thirdLine;
    if (c1 === 'hello' && c2 === 'hello') {
        thirdLine = '喵喵喵。';
    } else if (c1 === 'meow' && c2 === 'meow') {
        thirdLine = '干嘛三番五次套近乎？';
    } else {
        thirdLine = '喵三喵四喵喵喵？';
    }

    return [
        { choices: [
            { label: '没啥。', key: 'nothing' },
            { label: '看看你有没有受伤。', key: 'care' }
        ]},
        { text: thirdLine }
    ];
}
```

---

## 三、WorldMapRenderer 渲染变化

### 核心变化

现在 `_dialogue` 中每行可以是三种之一：
- 纯文本行：`string` 或 `{ text: "..." }`
- 纯选项行：`{ choices: [...] }`
- 混合行（旧格式兼容）：`{ text: "...", choices: [...] }`

渲染逻辑：

| 行类型 | 渲染内容 |
|--------|---------|
| `{ choices: [...] }` (无 text) | 只渲染选项按钮，对话框背景缩小，上方显示提示文字"你想说什么？" |
| `{ text: "...", choices: [...] }` | 旧格式兼容：台词 + 选项同时显示（不推荐新内容使用） |
| `{ text: "..." }` (无 choices) | 纯台词行，显示台词文字 + "点击继续" |

### 对话框尺寸自适应

选项行与台词行的高矮不同：

```
选项行高度 ≈ 按钮高度(38) + 上下边距(40) + 提示文字行高(19) ≈ 100 * uiScale
台词行高度 ≈ padding(20) + 文本行高(19*N) + 底部间距(30)
```

渲染时先检测当前行类型，计算对应高度。对话框底部对齐。

### 选项行渲染伪代码

```javascript
if (curLine && typeof curLine === 'object' && curLine.choices && !curLine.text) {
    // 纯选项行 — 只有选项按钮
    _choiceHotspots = [];

    // 提示文字
    var hintText = '你想说什么？';
    var hintFontSize = Math.floor(13 * uiScale);
    ctx.font = hintFontSize + 'px sans-serif';
    ctx.fillStyle = 'rgba(232,213,163,0.6)';
    ctx.textAlign = 'center';
    ctx.fillText(hintText, dlgX + dlgW / 2, dlgY + padding);

    // 选项按钮（在提示文字下方）
    var btnW = Math.floor((dlgW - padding * 3) / 2);
    var btnH = Math.floor(38 * uiScale);
    var btnY = dlgY + padding + Math.floor(22 * uiScale);  // 提示文字下方
    var btnGap = Math.floor(8 * uiScale);

    for (var bi = 0; bi < curLine.choices.length; bi++) {
        var btnX = dlgX + padding + bi * (btnW + btnGap);
        // ... 按钮背景、文字、热区绘制 ...
    }
}
```

---

## 四、推进逻辑变化

### advanceDialogue 流

原逻辑（单选行推进）：
```
advanceDialogue(key) → 通知回调 → index++ → 返回下一行
```

新逻辑（两段式）：
```
advanceDialogue(key) → 通知回调（更新状态）→ index++ → 返回"猫灵回应"行
用户点击继续 → advanceDialogue() → index++ → 到末尾 → dismissDialogue
```

换言之，选项行推进后**自动展示下一行**（猫灵回应），用户再点一次关闭对话。

`advanceDialogue` 代码不变，因为它本身就支持"推进后返回下一行"。关键是 `showDialogue` 的回调时机从"选项选择时"变成"选项选择时"（不变），回调内容从"更新状态+结束对话"变成"更新状态+对话继续到下一行（猫灵回应）"。

### _handleNpcDialogue 回调改造

```javascript
worldMapRenderer.showDialogue(script, function (choiceKey) {
    if (choiceKey) {
        var result = drv.onChoice(pd, driverId, state, choiceKey);
        if (result && result.toast) {
            $P.showToast({ title: result.toast, icon: 'success', duration: 2500 });
        }
        worldMapSystem.saveProgress();
    }
    // 注意：不在这里 dismissDialogue
    // advanceDialogue 会自动推进到下一行（猫灵回应），用户再点一次关闭
});
```

与旧版的关键区别：**回调中不再手动结束对话**。`advanceDialogue` 推进后自动展示猫灵回应的下一行，用户点击继续（或 E 键）后 `advanceDialogue()` 再推进到末尾，自动 dismiss。

---

## 五、E 键推进兼容

选项行中按 E：`advanceDialogue()` 无参 → 检测到 `cur.choices` 存在 → 返回 `cur`（不推进）。E 键无效，只能鼠标/触摸选择选项。

猫灵回应行中按 E：`advanceDialogue()` 无参 → 正常推进 → 到末尾 → dismiss。和当前行为一致。

触摸事件：选项行中只响应热区点击；回应行中触摸任意位置推进。

---

## 六、game.js 改动

与 `猫灵对话系统落地方案.md` 相同，唯一的差异在 `_handleNpcDialogue` 的回调函数：

```javascript
function _handleNpcDialogue(driverId) {
    var drv = _gameModules.NPC_DIALOGUE_DRIVERS[driverId];
    var pd = saveData;

    if (!pd.npcDialogState) pd.npcDialogState = {};
    if (!pd.npcDialogState[driverId]) {
        pd.npcDialogState[driverId] = drv.initState();
    }
    var state = pd.npcDialogState[driverId];

    var script = drv.getScript(state);
    if (!script || script.length === 0) return;

    worldMapRenderer.showDialogue(script, function (choiceKey) {
        if (choiceKey) {
            var result = drv.onChoice(pd, driverId, state, choiceKey);
            if (result && result.toast) {
                $P.showToast({ title: result.toast, icon: 'success', duration: 2500 });
            }
            worldMapSystem.saveProgress();
        }
        // 不手动 dismiss — advanceDialogue 自动推进到猫灵回应行
    });
}
```

---

## 七、与基本方案的差异对照

| | 基本方案 | 优化方案 |
|---|---|---|
| 对话脚本格式 | `[{ text, choices }]` 一行 | `[{ choices }, { text }]` 两行交替 |
| 交互节奏 | 台词+选项同时出现 | 先选、再看回应 |
| 推进次数 | 1次（选完即结束） | 2次（选→看回应→关闭） |
| WorldMapRenderer 改动 | 支持 `{ text, choices }` | 额外支持 `{ choices }` 无台词行 + 自适应高度 |
| E 键行为 | 选项行 E 键无效 | 相同 |
| 触摸行为 | 选项行只响应热区 | 相同 |
| onChoice 回调 | 更新状态 + 结束对话 | 更新状态，对话继续推进到下一行 |

---

## 八、向后兼容

- `{ text: "...", choices: [...] }` 格式仍渲染为台词+选项（旧格式兼容）
- 纯字符串数组 `["...", "..."]` 仍正常工作（world_04 商户NPC）
- `{ choices: [...] }` 无 text → 新格式，只渲染选项
- `{ text: "..." }` 无 choices → 纯台词行，和旧字符串行等价

---

## 九、实施步骤

在 `猫灵对话系统落地方案.md` 六步基础上，额外改动：

1. `NpcDialogueConfig.js` — 猫灵 driver 的 `getScript` 改为两段式脚本（如第二节所示）
2. `WorldMapRenderer.js` — 选项行渲染支持 `{ choices }`（无 text）格式；对话框高度自适应；选项行显示提示文字
3. `game.js` — `_handleNpcDialogue` 回调中去掉手动 dismiss 逻辑