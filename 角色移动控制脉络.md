# 角色移动控制脉络

## 总览

角色移动分两大模式：**大世界自由移动** 和 **无尽之塔网格移动**。战斗状态下无角色自由移动，只有战斗操作。

---

## 数据流向

```
输入捕获 (game.js)
├── 键盘: keydown/keyup → _keysDown = {w:true, a:false, ...}
├── 摇杆: touchstart/move/end → _joystickActive, _joystickDX/DY
│
▼ 每帧 update
├── [WORLDMAP 状态] 键盘方向 + 摇杆偏移 → worldMapSystem.movePlayer(dx, dy, dt)
│   └── WorldMapPlayer.movePlayer()
│       ├── 归一化方向 (单位向量)
│       ├── newPos = pos + dir × 200px/s × dt
│       ├── 边界 clamp + 轴分离碰撞检测
│       │   ├── 像素位图碰撞 (_collisionBitmap)
│       │   ├── 瓦片网格碰撞 (_tileGrid)
│       │   └── 矩形列表碰撞 (_activeCollisions)
│       └── 反卡死: _pushOutOfCollision 8方向扫描脱出
│
├── [TOWER 状态] 键盘方向 → towerSystem.movePlayer(tdx, tdy)
│   └── 网格步进移动 (150ms 冷却，每次移动 1 格)
│
├── 传送 (WorldMapSystem.update)
│   ├── teleport 实体: 走进半径 → setPlayerPos(targetX, targetY)
│   ├── portal 实体: E键/点击 → loadWorld(目标世界, portal坐标)
│   └── triggerLine: 跨越触发线 → loadWorld(目标世界, playerStart)
│
▼ 渲染
└── WorldMapRenderer
    ├── updateCamera() — 摄像机平滑跟随角色 (lerp 0.1)
    └── 绘制角色 + 摇杆指示器
```

---

## 关键数据结构

### 输入状态 (game.js 作用域)

| 变量 | 类型 | 说明 |
|------|------|------|
| `_keysDown` | `{}` | 键盘按键状态，`{w:true, d:false, ...}` |
| `_joystickActive` | `bool` | 摇杆是否激活 |
| `_joystickStartX/Y` | `number` | 摇杆触摸起点 |
| `_joystickDX/DY` | `number` | 摇杆偏移量 |
| `_towerKeyMoveCooldown` | `number` | 塔移动冷却计时 (150ms) |

### 角色状态 (WorldMapPlayer)

| 字段 | 类型 | 说明 |
|------|------|------|
| `_pos` | `{x, y}` | 角色世界坐标 |
| `_speed` | `200` | 移动速度 (像素/秒) |
| `_playerRadius` | `12` | 碰撞半径 |
| `_activeCollisions` | `[]` | 动态矩形碰撞体列表 |
| `_tileGrid` | `{tileSize, gridW, gridH, grid[][]}` | 瓦片碰撞网格 |
| `_collisionBitmap` | `{width, height, data: Uint8Array}` | 像素级碰撞位图 |
| `_returnPosition` | `{x, y}` | 离开大世界时的位置快照 |
| `_spawnPoint` | `{x, y}` | 出生点 |
| `_currentFloor` | `number` | 当前楼层 (切换碰撞遮罩) |

---

## 移动模式总表

| 模式 | 状态 | 输入 | 移动方式 | 碰撞 | 速度 |
|------|------|------|----------|------|------|
| 大世界自由移动 | WORLDMAP | WASD + 虚拟摇杆 | 像素平滑 | 位图+瓦片+矩形 | 200px/s |
| 无尽之塔 | TOWER | WASD | 网格步进 | 塔格子 | 1格/150ms |
| teleport 传送 | WORLDMAP内 | 自动(走进半径) | 瞬移到目标坐标 | — | 瞬移 |
| portal 传送 | WORLDMAP | E键/点击 | 切换世界 | — | 世界切换 |
| triggerLine | WORLDMAP | 自动(跨越) | 切换世界 | — | 世界切换 |
| 楼层切换 | WORLDMAP | 走传送点 | 切换碰撞遮罩 | — | 换碰撞层 |
| 战斗 | PLAYING等 | 触摸点击/拖拽 | 无角色移动 | — | — |

---

## 模块关系

```
game.js (总控)
├── 输入捕获 → _keysDown / _joystick*
├── update loop ──→ WorldMapSystem.movePlayer()
│                └──→ TowerSystem.movePlayer()
├── WorldMapPlayer (碰撞+位置)
├── WorldMapRenderer (摄像机+绘制)
├── PauseCoordinator (暂停调度)
│   ├── AnimationSystem 冻结时钟
│   └── BattleEngine 补偿定时器
├── AudioSystem (步声音效)
└── 位置快照 (离开→_returnPosition / 返回→恢复)
```

---

## 与暂停系统的交互

- 大世界移动（WORLDMAP）**不直接受 PauseCoordinator 控制**
- 战斗暂停通过 `PauseCoordinator` 发布-订阅通知 BattleEngine 和 AnimationSystem
- AnimationSystem 使用虚拟冻结时钟：暂停期间 `getGameTime()` 不变，动画静止
- 塔模式切战斗（TOWER→TOWER_COMBAT）时自动屏蔽塔移动输入

---

## 关键文件索引

| 文件 | 职责 |
|------|------|
| `V1/game.js` | 总入口：输入捕获、每帧 update 分发、渲染循环 |
| `V1/src/systems/WorldMapSystem.js` | 大世界系统：组合 Player/Entity，透传移动，传送/触发线检测 |
| `V1/src/systems/worldmap/WorldMapPlayer.js` | 移动+碰撞核心：坐标管理、碰撞检测、防卡死 |
| `V1/src/systems/TowerSystem.js` | 无尽之塔网格移动 |
| `V1/src/renderers/WorldMapRenderer.js` | 摄像机跟随、角色绘制、摇杆指示器 |
| `V1/src/config/WorldMapConfig.js` | 地图数据、碰撞遮罩、playerStart、实体列表 |
| `V1/src/config/CollisionBitmapConfig.js` | 预计算碰撞位图 (RLE编码) |
| `V1/src/config/TeleportConfig.js` | 传送点/传送门/触发线/出生点坐标 |
| `V1/src/utils/PauseCoordinator.js` | 统一暂停调度器 |
| `V1/src/systems/AnimationSystem.js` | 虚拟冻结时钟、步声音效 |
| `V1/暂停脉络汇总.md` | 暂停状态汇总文档 |
| `V1/大世界探索系统策划案.md` | 状态流图、多大世界衔接规则 |