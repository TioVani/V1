### 核心概念

图鉴系统是一套跨实体的**收集进度记录与奖励体系**。玩家通过战斗收服、融合产出、扭蛋抽取、任务获取、掉落等途径获得新实体时，自动在图鉴中点亮对应条目，并获得收集奖励。图鉴本身不存储实体实例，只记录首次获得时间和轻度追踪统计（累计获得次数、最高星级等）。

### 收录实体类型

| 图鉴分册 | type 标识 | 配置来源 | 实体数 | 获得途径 |
|---------|----------|---------|-------|---------|
| 古灵图鉴 | `character` | `CharacterConfig` | 26 | 战斗收服灵光 |
| 灵兽图鉴 | `pet` | `PetConfig` | 35+ | 扭蛋 / 商城 / 融合 / 怪物掉落 |
| 灵器图鉴 | `equipment` | `EquipmentConfig` | 50+ | 战斗掉落 / 商城 / 融合 / 任务 |
| 灵术图鉴 | `skill` | `SkillConfig` | 45+ | 扭蛋 / 融合 / 掉落 |
| 邪灵图鉴 | `monster` | `MonsterConfig` | 48+ | 战斗遭遇 |
| 融合图鉴 | `fusion` | `FusionConfig` | 动态 | 融合产出 |

**不收录**：材料（`MaterialConfig`）作为消耗品不单独设图鉴分册。

**与邪灵图鉴的边界**：邪灵图鉴记录的是遭遇/击败过的敌方怪物（`MonsterConfig` 中的条目），判定条件为**首次击败**——当战斗引擎处理怪物死亡时触发记录。如果一只怪物被收服成为古灵，它同时计入古灵图鉴（通过收服逻辑）和邪灵图鉴（通过击败逻辑）两个分册，二者不互斥。

### type 标识与分册名映射

CodexSystem 内部使用 type 字符串标识分册，渲染层通过映射表转为显示名：

| type | 分册显示名 | 图标（emoji 占位） | Config 引用 |
|------|-----------|-------------------|-------------|
| `character` | 古灵图鉴 | 🔶 | `CharacterConfig` |
| `pet` | 灵兽图鉴 | 🐾 | `PetConfig` |
| `equipment` | 灵器图鉴 | ⚔️ | `EquipmentConfig` |
| `skill` | 灵术图鉴 | 📜 | `SkillConfig` |
| `monster` | 邪灵图鉴 | 👹 | `MonsterConfig` |
| `fusion` | 融合图鉴 | 🧬 | `FusionConfig` |

### CodexSystem deps 与初始化

系统实例化时通过 deps 注入 Config 映射，使 `getProgress`/`getEntries` 能动态读取各分册总条目数：

```javascript
var codexSystem = createCodexSystem({
    getSaveData: function() { return saveData; },
    saveData: saveData,
    showToast: function(opts) { /* Toast逻辑 */ },
    configs: {
        character: Characters,      // 即 CharacterConfig 导出对象
        pet: Pets,                  // PetConfig
        equipment: Equipments,      // EquipmentConfig
        skill: Skills,              // SkillConfig
        monster: Monsters,          // MonsterConfig
        fusion: {}                  // FusionConfig（fusion 分册 total=0，仅作类型标记）
    }
});
```

### CodexSystem API

| 方法 | 说明 |
|------|------|
| `unlock(type, id, opts?)` | 幂等解锁条目。opts 可选：`{ starLevel, obtainCount, silent }`。`silent: true` 时跳过里程碑弹窗（迁移用） |
| `updateLoreTier(configId, tier)` | 提升古灵的文物小传层级。`configId` 为配置 ID（如 `char_001`），仅 character 类型有效 |
| `getProgress(type)` | 获取分册进度 `{ unlocked, total, percent }`。total 从 `deps.configs[type]` 动态读取；fusion 分册 total=0 |
| `getTotalProgress()` | 获取总进度（排除 fusion 分册），返回 `{ unlocked, total, percent }` |
| `claimMilestone(type, milestone)` | 领取并标记分册里程碑奖励 |
| `isMilestoneClaimed(type, milestone)` | 检查里程碑是否已领取 |
| `getEntries(type)` | 获取分册条目列表。非 fusion 分册：遍历 `deps.configs[type]` 生成全量条目；fusion 分册：仅返回已解锁的条目 |

### 稀有度体系统一

沿用现有 8 级体系（`GameConfig.js` → `RARITY_COLORS`）：

```
UC(粗品) → N(凡品) → R(良品) → SR(珍品) → SSR(瑰宝) → UR(国宝) → LR(传世) → SP(镇馆之宝)
```

注意：古灵图鉴中实际不存在 UC 级条目（UC 仅出现在灵兽/灵器配置中），进度计算以各分册实际配置的稀有度范围为基准。

### 图鉴条目数据结构

```
CodexEntry {
  id: "char_001",                    // 实体配置ID（非实例 uid）
  type: "character",                 // character/pet/equipment/skill/monster/fusion
  unlocked: true,                    // 是否已获得过
  unlockTime: 1711234567890,         // 首次获得时间戳
  obtainCount: 3,                    // 累计获得次数（轻度追踪，非实例存储）
  starLevel: 4,                      // 当前持有最高星级（如适用）
  loreTier: 1,                       // 已解锁的文物小传层级（仅 character，0=未解锁, 1=小传, 2=灵语, 3=秘闻）
}
```

所有 `id` 字段均为**配置 ID**（如 `char_001`、`pet_slime`），非实例 `uid`。外部调用 `unlock()` 时需确保传入的是配置 ID——收服系统产出的是配置 ID，融合系统产出的是配置 ID，灵兽/灵器/灵术系统同理。

### 存储结构

```javascript
playerData.codex = {
    entries: [
        { id: "char_001", type: "character", unlocked: true, unlockTime: 1711234567890, obtainCount: 3, starLevel: 4, loreTier: 1 },
        // ...
    ],
    milestones: {
        // key = type, value = 已领取的百分位节点数组（如 [10, 25]）；使用 Set 语义去重
        "character": [10, 25],
        "pet": [10],
        // 未领取任何里程碑的分册可以不出现此 key
    }
};
```

`GameDataStore.js` 的 `defaultSaveData` 中新增：

```javascript
codex: { entries: [], milestones: {} }
```

`unlock()` 幂等性：同 type + id 组合多次调用只记录首次的 unlockTime，但会累加 obtainCount 和更新 starLevel（取 max）。

### 文物小传三层结构

仅古灵图鉴附带，沿用《想法02.md》中规划的三层解锁：

| 层级 | loreTier | 解锁条件 | 触发方 | 内容 | 文字量 |
|------|----------|---------|-------|------|-------|
| 小传 | 1 | 收服即解锁 | `CodexSystem.unlock()` 内部自动设为 1 | 文物真实历史背景 | ~100 字 |
| 灵语 | 2 | 升级到 30 级 | `UpgradeCharacterStrategy` 升级后检测 | 古灵性格台词、与其他古灵的对话彩蛋 | ~200 字 |
| 秘闻 | 3 | 升星到 ★★★ | `UpgradeEngine.starUpgrade()` 成功回调中检测 | 隐藏故事片段，揭示古灵的前世今生 | ~500 字 |

`CharacterConfig` 中新增 `lore_tier1`/`lore_tier2`/`lore_tier3` 三个字段存储对应层级文本。第一阶段仅实装 `lore_tier1`（小传），后续层级按版本迭代追加内容。`updateLoreTier()` 只能升不能降（内部 `loreTier = max(loreTier, newTier)`）。

**uid → configId 解析**：`UpgradeCharacterStrategy` 和 `UpgradeEngine.starUpgrade()` 操作的是实例 `uid`，需通过策略的 `resolveEntity(uid)` 取到实体对象，再通过实体的 `id` 字段（配置 ID）传给 `updateLoreTier()`。

### 进度计算与奖励

**分册进度**：
- 该分册已解锁条目数 / 该分册总条目数（总条目数从 `deps.configs[type]` 动态读取）
- 融合分册（`fusion`）total = 0，不参与百分比计算，仅展示已发现数量

**里程碑触发规则**：
- 里程碑阈值按 `ceil(分册总条目数 × 百分比)` 计算，向上取整（确保至少收集到该数量才触发）
  - 例：古灵图鉴 26 个条目，10% 阈值 = ceil(2.6) = 3，25% 阈值 = ceil(6.5) = 7
- 每次 `unlock()` 成功后，比对分册当前已解锁数与各阈值，若跨越了未领取的节点则触发弹窗
- 领取后写入 `codex.milestones[type].push(milestonePercent)`，用数组 + `indexOf` 实现去重

**总收集进度**：
- 排除 fusion 分册，公式：count(所有非 fusion 分册已解锁条目) / count(所有非 fusion 分册总条目数)
- 每 20% 触发一次全服奖励（限时头像框、限定灵光等）

**里程碑奖励表**（道具名称待后续与道具体系对齐后实装，当前仅做占位设计）：

| 分册 | 10% | 25% | 50% | 75% | 100% |
|------|-----|-----|-----|-----|------|
| 古灵 | 灵光石×5 | 灵光石×10 | 限定称号"考古者" | 灵光石×20 | 限定头像框 |
| 灵兽 | 唤灵石×3 | 唤灵石×5 | 灵兽粮×10 | 唤灵石×10 | 限定灵兽皮肤 |
| 灵器 | 强化石×5 | 强化石×10 | 精炼符×3 | 强化石×20 | 限定灵器外观 |
| 灵术 | 灵术卷轴×2 | 灵术卷轴×5 | 灵术卷轴×10 | 灵术卷轴×15 | 限定技能特效 |
| 邪灵 | 金币×500 | 金币×1000 | 经验丹×5 | 金币×2000 | 限定称号"除灵师" |
| 融合 | 无 | 无 | 无 | 无 | 无 |

### 首次获得提示

获得新实体时，以 Toast 形式弹出提示：

```
┌──────────────────────────┐
│  ✨ 图鉴解锁！           │
│                          │
│  [实体图标] [实体名称]    │
│  稀有度: [SSR 瑰宝]      │
│  古灵图鉴: 12/26         │
│                          │
│  [文物小传预览文字...]    │
└──────────────────────────┘
```

提示持续 3 秒，点击可跳转到图鉴详情页。

### 与现有系统的交互

```
CaptureSystem          ──(收服成功)──→ codex.unlock("character", id)
   │  id = spirit.id（配置ID）              │
GachaSystem           ──(扭蛋产出)──→ codex.unlock("pet"|"skill", id)
   │                                       │
BattleEngine          ──(击败怪物)──→ codex.unlock("monster", id)
   │                                       │
FusionEngine          ──(融合产出)──→ codex.unlock(type, resultId)
   │  type ∈ {"character","pet","equipment","skill"}
   │
FusionRegistry        ──(融合记录)──→ codex.unlock("fusion", resultId)
   │                                       │
ShopSystem            ──(购买获得)──→ codex.unlock("pet"|"equipment", id)
   │                                       │
TaskSystem            ──(任务奖励)──→ codex.unlock(type, id)
   │                                       │
DropSystem            ──(怪物掉落)──→ codex.unlock("pet"|"equipment", id)
   │                                       │
UpgradeCharacterStrategy ──(升到30级)──→ resolveEntity(uid).id → codex.updateLoreTier(configId, 2)
   │                                       │
UpgradeEngine.starUpgrade() ──(升到★★★)──→ resolveEntity(uid).id → codex.updateLoreTier(configId, 3)
   │  仅在 type === UPGRADE_TYPES.CHARACTER（即 "character"）且 newStarLevel >= 3 时触发
```

**`UpgradeEngine.starUpgrade()` 类型验证**：`UpgradeConfig.js` 中 `UPGRADE_TYPES.CHARACTER = 'character'`，与 CodexSystem 的 type 一致。判断条件：`type === 'character' && newStarLevel >= 3`。

### 与已有融合图鉴的关系

`FusionRegistry.js` 已有一个融合产物简易图鉴（`fusionData.codex`），按融合类型分册，只存 `id[]`。

**决策：CodexSystem 作为主图鉴系统，融合图鉴作为 `fusion` 分册整合进 CodexSystem。**

具体做法：
- `FusionRegistry.record()` 内部改为调用 `codex.unlock("fusion", resultId)`
- 已有融合图鉴数据（`fusionData.codex`）在 CodexSystem 首次初始化时迁移到 `playerData.codex.entries`，迁移完成后清除旧字段
- 融合分册无预定总数，不设里程碑奖励，仅做收集进度展示

### 图鉴 UI 入口

**主入口**：主菜单（`MenuRenderer.js`）新增"图鉴"按钮，与现有的"背包""编队""商城"等并列。点击进入图鉴总览页。

**快捷入口**：
- 融合界面（`FusionRenderer.js`）现有的"📖图鉴"按钮改为跳转到 CodexRenderer 总览页，而非继续使用融合专属的简易图鉴。该按钮在融合图鉴迁移完成后移除旧逻辑。
- 首次获得 Toast 提示可点击跳转至对应分册详情页。

### 生命周期状态机

```
                 首次获得
  [未解锁] ──────────────────→ [已解锁]  (loreTier 自动设为 1)
      │                            │
      │                            │ 再次获得
      │                            ↓
      │                      obtainCount += 1
      │                      starLevel = max(starLevel, newStarLevel)
      │                            │
      │                            │ 升级/升星
      │                            ↓
      │                      updateLoreTier(configId, tier)  // 外部调用
      │                            │
      │                            │ 达到里程碑
      │                            ↓
      │                      claimMilestone(type, percent) → 发放奖励
      │
      └── 始终不可见/灰色
```

### 图鉴 UI 布局

**总览页**（竖屏单列滚动列表）：

```
┌─────────────────────────────────────┐
│  总收集进度: ████████░░  42.3%      │
│  (不含融合分册)                     │
│─────────────────────────────────────│
│  ┌─────────────────────────────────┐│
│  │ 🔶 古灵图鉴    12/26  46% ██░░  ││
│  └─────────────────────────────────┘│
│  ┌─────────────────────────────────┐│
│  │ 🐾 灵兽图鉴    18/35  51% ███░  ││
│  └─────────────────────────────────┘│
│  ┌─────────────────────────────────┐│
│  │ ⚔️ 灵器图鉴    23/50  46% ██░░  ││
│  └─────────────────────────────────┘│
│  ┌─────────────────────────────────┐│
│  │ 📜 灵术图鉴    15/45  33% █░░░  ││
│  └─────────────────────────────────┘│
│  ┌─────────────────────────────────┐│
│  │ 👹 邪灵图鉴    30/48  62% ███░  ││
│  └─────────────────────────────────┘│
│  ┌─────────────────────────────────┐│
│  │ 🧬 融合图鉴    已发现 8 种       ││
│  └─────────────────────────────────┘│
└─────────────────────────────────────┘
```

**分册详情页**（非 fusion 分册，4 列网格）：

```
┌─────────────────────────────────────┐
│  ← 古灵图鉴      进度: 12/26 (46%)  │
│─────────────────────────────────────│
│  ┌──┐ ┌──┐ ┌──┐ ┌──┐ ┌──┐          │
│  │✓│ │✓│ │✓│ │  │ │✓│ │  │  ...   │
│  │甲│ │乙│ │丙│ │?│ │戊│ │?│        │
│  └──┘ └──┘ └──┘ └──┘ └──┘          │
│  (4列网格，已解锁显示图标+名称，     │
│   未解锁显示?+灰色剪影)              │
│  按稀有度从低到高排列               │
│─────────────────────────────────────│
│  选中条目详情:                       │
│  [大图标] 名称: 青铜鼎灵             │
│  稀有度: LR 传世                     │
│  获得次数: 3                        │
│  文物小传: [lore_tier1 内容]         │
│  灵语:     [lore_tier2 内容]（30级解锁）│
│  秘闻:     [lore_tier3 内容]（★★★解锁）│
└─────────────────────────────────────┘
```

**融合分册详情页**（仅已解锁条目列表，无网格/未解锁占位）：

```
┌─────────────────────────────────────┐
│  ← 融合图鉴        已发现 8 种      │
│─────────────────────────────────────│
│  ✓ [产物图标] 乾坤鎏金青花剑  SSR   │
│  ✓ [产物图标] 青铜鼎·共进  SR       │
│  ✓ [产物图标] ...                   │
│  (单列滚动列表，按发现时间倒序)      │
└─────────────────────────────────────┘
```

稀有度用边框颜色区分：`#7CCD7C`(UC)→`#aaaaaa`(N)→`#4A90D9`(R)→`#9B59B6`(SR)→`#FFD700`(SSR)→`#E74C3C`(UR)→`#FF6B9D`(LR)→`#00D4FF`(SP)。

### 系统实例化

项目采用**工厂函数 + 闭包 deps 注入**模式（无中心化 DI 容器）。CodexSystem 的创建和初始化需在两个文件中完成：

**`game.js`（系统实例化入口）**：
```javascript
// 实例化
var codexSystem = createCodexSystem({
    getSaveData: function() { return saveData; },
    saveData: saveData,
    showToast: function(opts) { /* Toast逻辑 */ },
    configs: {
        character: Characters,
        pet: Pets,
        equipment: Equipments,
        skill: Skills,
        monster: Monsters,
        fusion: {}
    }
});

// 初始化（必须在 GameDataStore.load() 之后）
codexSystem.init();  // 内部执行迁移步骤 1~4
```

**`entry.js`（Rollup 打包入口）**：
```javascript
import { createCodexSystem } from './systems/CodexSystem.js';
// 挂载到 GameModules 供 game.js 引用
GameModules.CodexSystem = { createCodexSystem: createCodexSystem };
```

### 新增/修改文件清单

| 文件 | 操作 | 说明 |
|------|------|------|
| `src/systems/CodexSystem.js` | 新增 | 图鉴核心逻辑：unlock/updateLoreTier/getProgress/getTotalProgress/claimMilestone/init（迁移） |
| `src/renderers/CodexRenderer.js` | 新增 | 图鉴 UI 渲染（总览单列列表 + 分册4列网格/融合列表 + 条目详情） |
| `src/config/CharacterConfig.js` | 修改 | 每个古灵条目新增 `lore_tier1`/`lore_tier2`/`lore_tier3` 字段 |
| `src/systems/CaptureSystem.js` | 修改 | 收服成功后调用 `codex.unlock("character", spirit.id)` |
| `src/systems/GachaSystem.js` | 修改 | 扭蛋产出后调用 `codex.unlock()` |
| `src/systems/BattleEngine.js` | 修改 | 怪物被击败时调用 `codex.unlock("monster", id)` |
| `src/systems/fusion/FusionEngine.js` | 修改 | 融合成功后调用 `codex.unlock(type, resultId)` |
| `src/systems/FusionRegistry.js` | 修改 | `record()` 桥接到 `codex.unlock("fusion", resultId)`；`init()` 内部迁移旧数据 |
| `src/systems/upgrade/strategies/UpgradeCharacterStrategy.js` | 修改 | 升级后通过 `resolveEntity(uid)` 取 `entity.id`，检测达 30 级则调用 `codex.updateLoreTier(id, 2)` |
| `src/systems/upgrade/UpgradeEngine.js` | 修改 | `starUpgrade()` 成功后若 `type === "character"` 且 `newStarLevel >= 3`，通过 `resolveEntity(uid)` 取 `entity.id` 调用 `codex.updateLoreTier(id, 3)` |
| `src/systems/ShopSystem.js` | 修改 | 购买获得后调用 `codex.unlock()` |
| `src/systems/TaskSystem.js` | 修改 | 任务奖励实体时调用 `codex.unlock()` |
| `src/systems/DropSystem.js` | 修改 | 怪物掉落灵兽/灵器时调用 `codex.unlock()` |
| `src/systems/GameDataStore.js` | 修改 | `defaultSaveData` 新增 `codex: { entries: [], milestones: {} }` |
| `src/systems/PlayerDataSystem.js` | 修改 | save 模板新增 `codex` 字段 |
| `src/renderers/MenuRenderer.js` | 修改 | 主菜单新增"图鉴"按钮，点击跳转 CodexRenderer 总览页 |
| `src/renderers/FusionRenderer.js` | 修改 | "📖图鉴"按钮改为跳转 CodexRenderer 总览页 |
| `game.js` | 修改 | 实例化 `codexSystem = createCodexSystem({...})` + 调用 `codexSystem.init()`（在 load 之后） |
| `src/entry.js` | 修改 | import CodexSystem 并挂载到 `GameModules`（不做初始化） |

### 分阶段实施

| 阶段 | 内容 |
|------|------|
| 第一阶段 | CodexSystem 核心逻辑（unlock/updateLoreTier/进度计算/里程碑判定/迁移 init）+ 古灵图鉴接入 + 融合图鉴迁移 |
| 第二阶段 | CodexRenderer UI + MenuRenderer 主入口按钮 + FusionRenderer 按钮改造 |
| 第三阶段 | 灵兽/灵器/灵术/邪灵 四个分册接入 + DropSystem/FusionEngine/UpgradeCharacterStrategy/UpgradeEngine 接入 |
| 第四阶段 | 文物小传 `lore_tier1` 数据填充（26 位古灵）+ 里程碑奖励实装（此时道具体系已对齐） |

### 数据迁移

`CodexSystem.init()` 在 `game.js` 中 `GameDataStore.load()` 之后调用，依次执行：

**步骤 1 — 确保 codex 字段存在**：若 `playerData.codex` 未定义，初始化为 `{ entries: [], milestones: {} }`。

**步骤 2 — 迁移古灵已收集数据**：遍历 `playerData.capturedSpirits[]`（若存在），逐条调用 `codex.unlock("character", spirit.id, { silent: true })`。`spirit.id` 即配置 ID。`silent: true` 压制迁移期间的里程碑弹窗。由于 `unlock()` 幂等且 `capturedSpirits` 按 id 查重，不会产生重复条目。`unlockTime` 使用当前时间，`obtainCount` 设为 1。

**步骤 3 — 迁移融合图鉴旧数据**：读取 `playerData.fusionData.codex`（若存在），遍历各 type 下的 `id[]` 数组，逐条调用 `codex.unlock("fusion", id, { silent: true })`。迁移完成后删除 `fusionData.codex` 字段。

**步骤 4 — 回填里程碑**：迁移完成后，对所有已解锁条目计算各分册收集率。里程碑阈值按 `ceil(total × percent)` 计算。对于已达到但未领取的里程碑节点（10%/25%/50%/75%/100%），自动标记为已领取（写入 `codex.milestones`），但**不补发奖励**——老玩家只获得里程碑记录，不回溯发放历史奖励。若需要补偿，由运营侧另行处理。