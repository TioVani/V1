# 怪物攻击脉络

## 一、攻击流程总览

```
怪物生成（MonsterSpawnSystem）
  │  计算 HP / 攻击力 / 技能
  ▼
注册为攻击者（BattleEngine.addAttacker）
  │  将怪物加入 attackers[] 数组
  ▼
攻击循环（BattleEngine.update → updateAttackers）
  │  每帧遍历 attackers[]，检查攻击间隔是否到期
  ▼
发射弹幕（BattleEngine.fireAttackerProjectile）
  │  动画层：createMonsterProjectile（500ms 飞行）
  ▼
弹幕命中回调（onHit）
  ├─ 闪避检查 → 反击伤害
  ├─ 护盾吸收 → playerShield 先扣
  ├─ HP 扣减 → playerHp -= damage
  ├─ 扣时间 → timeLeft -= timeDamageOnHit（5秒/次）
  ├─ 中毒附加 → 检查 monster.skills[].type === 'poison'
  ├─ 眩晕附加 → 检查 monster.skills[].type === 'stun'
  └─ Boss眩晕 → bossStunChance 概率触发
  ▼
战斗结束判定
  ├─ playerHp <= 0 → finishBattle('playerDeath')
  └─ timeLeft <= 0 → finishBattle('timeUp')
```

---

## 二、怪物伤害公式（怪物→玩家）

```
基础伤害 = monster.attack（配置的 baseAttack）
  × rageMultiplier       （狂暴加成，默认 1，狂暴后 1.5~2.5）
  × 2                     （双倍攻击 doubleAtk 触发时）
  × 1.5（Boss）/ 2.0（普通） （score ≥ 2000 时的惩罚系数）
  + damageBonus           （觉醒加成）
```

**受击效果：**
- 护盾先吸收：`shield -= min(shield, damage)`，剩余扣 HP
- 时间惩罚：`timeLeft -= 5秒`（每次受击固定扣 5 秒）
- 动画：震动 + 飘字 "-X 灵能"

---

## 三、攻击者管理机制

| 操作 | 说明 |
|------|------|
| `addAttacker(monster, interval)` | 注册怪物到 `attackers[]`，可自定义攻击间隔 |
| `removeAttacker(monsterId)` | 怪物死亡后移除 |
| `updateAttackers()` | 每帧遍历，到达间隔时调用 `fireAttackerProjectile` |
| `clearAttackers()` | 战斗结束/销毁时清空 |

**关键参数：**
- 默认攻击间隔：**2000ms**（`COMBAT_SPEC.MONSTER.ATTACK_INTERVAL_MS`）
- 弹幕飞行时间：**500ms**（`COMBAT_SPEC.VFX.MONSTER_PROJECTILE_MS`）
- 每个怪物可拥有独立的 `attackInterval` 覆盖默认值

---

## 四、13 种怪物技能

### 4.1 受击触发类（怪物攻击命中玩家时触发）

| 技能 | 效果 | 典型怪物 |
|------|------|----------|
| **中毒 (poison)** | 攻击附带中毒，每秒跳伤 N 点，持续 N 秒 | 锈灵碎片、丹青卷邪灵 |
| **眩晕 (stun)** | N% 概率打断玩家操作 N 毫秒 | 狂草邪灵、铜龙纹邪灵、鎏金邪灵 |
| **双倍攻击 (doubleAtk)** | N% 概率本次伤害 ×2 | 青瓷邪灵、剑意邪灵 |

### 4.2 防御/被动类（玩家攻击怪物时触发）

| 技能 | 效果 | 触发条件 |
|------|------|----------|
| **闪避 (dodge)** | N% 概率闪避玩家攻击 | 玩家点击星星命中时 |
| **吸收 (absorb)** | 吸收指定类型星星伤害回血 | 玩家点击对应类型星星时 |
| **反弹 (reflect)** | 反弹 N% 伤害给玩家 | 玩家造成伤害时 |
| **护甲 (armor)** | 固定减伤 N% | 持续生效 |
| **护盾 (shield)** | 每 N 秒获得护盾吸收伤害 | 定时触发 |

### 4.3 特殊机制类

| 技能 | 效果 | 触发条件 |
|------|------|----------|
| **狂暴 (rage)** | HP 降至阈值以下时攻击力 ×N | HP ≤ N% |
| **分裂 (split)** | HP 降至阈值以下时分裂成 N 只小怪 | HP ≤ N%（仅 Boss） |
| **召唤 (summon)** | 每 N 秒召唤 N 只指定怪物 | 定时触发 |
| **治疗 (heal)** | 每 N 秒回复 N HP | 定时触发 |
| **毒液溅射 (poison_split)** | HP 降至阈值时溅射毒液 + 生成毒液滩 | HP ≤ N%（仅 Boss） |

---

## 五、稀有度 × 攻击能力矩阵

| 稀有度 | 怪物数量 | 攻击力范围 | 攻击间隔范围 | 技能数 | 典型攻击威胁 |
|--------|----------|------------|--------------|--------|--------------|
| **UC** | 3 种 | 4~6 | 2000~2500ms | 0 | 几乎无威胁 |
| **N** | 6 种 | 6~12 | 1500~2500ms | 0~1 | 基础威胁，锈灵碎片有中毒 |
| **R** | 5 种 | 12~25 | 1200~2500ms | 1 | 闪避/连击/护盾/治疗/双倍攻击 |
| **SR** | 4 种 | 20~45 | 1600~3000ms | 1~2 | 狂暴×1.5 / 35%闪避+20%反弹 / 30%减伤 / 15%眩晕 |
| **SSR** | 4 种 | 40~70 | 1000~2200ms | 2 | 召唤+中毒 / 眩晕+吸收 / 40%闪避+25%连击 / 眩晕+吸收 |
| **UR** | 2 种 | 80~100 | 1000~1200ms | 3 | 眩晕+狂暴+减伤 / 闪避+反弹+吸收所有 |

---

## 六、Boss 攻击特化

### 6.1 Boss 独有机制

- **Boss 眩晕**：可通过 `bossStunChance` 参数，在攻击命中时有概率额外眩晕玩家
- **多阶段**：通过 `bossPhase` 标记（1~9），从入门到终局
- **独立配置**：`BOSS_LIST` 拥有独立 HP / 攻击力 / 间隔覆盖值

### 6.2 Boss 攻击参数速查

| Boss | HP | 攻击力 | 攻击间隔 | BossPhase | 核心攻击技能 |
|------|-----|--------|----------|-----------|--------------|
| 聚合邪灵·瓷 | 800 | 30 | 2500ms | 1 | 分裂(≤50%) + 毒液溅射(≤70%) |
| 统领邪灵·戈 | 1200 | 50 | 2000ms | 2 | 召唤戈魂 + 吸收水灵星 |
| 烈焰鼎邪灵 | 2000 | 80 | 1800ms | 3 | 眩晕 + 吸收火灵星 + 狂暴×2(≤30%) |
| 冰裂瓷邪灵 | 1800 | 70 | 2000ms | 4 | 眩晕 + 吸收水灵星 + 护盾(+100/8s) |
| 雷击剑邪灵 | 3000 | 100 | 1500ms | 5 | 眩晕 + 30%连击 + 吸收金灵星 |
| 灵脉邪灵 | 5000 | 150 | 1200ms | 6 | 眩晕 + 吸收所有 + 召唤灵脉裂片 + 闪避 |
| 古器邪灵 | 8000 | 200 | 1000ms | 7 | 眩晕 + 狂暴×1.8(≤50%) + 40%反弹 + 30%减伤 |
| 渊默邪灵 | 15000 | 300 | 800ms | 8 | 眩晕3s + 吸收50%所有 + 召唤+狂暴×2 + 40%减伤（最终Boss） |
| 万灵聚合体 | 20000 | 250 | 800ms | 9 | 眩晕 + 吸收50%所有(全回血) + 狂暴×2.5(≤30%) + 30%反弹（最终Boss） |

### 6.3 特殊 Boss

| Boss | 攻击力 | 攻击间隔 | 特点 |
|------|--------|----------|------|
| 灵脉吞噬者 | 120 | 1500ms | HP<30%逃跑；眩晕+吸收所有；专属掉落池 |
| 窃灵者 | 15 | 2000ms | `star_thief` 类型，攻击力低但特殊机制 |

---

## 七、攻击流程中的扩展钩子

`BattleEngine` 在攻击链路中暴露多个扩展点，由 `NormalBattleAdapter` / `BossBattleAdapter` 注入模式特有逻辑：

| 钩子 | 调用时机 | 作用 |
|------|----------|------|
| `onBeforeStarClick` | 玩家点击星星前 | 拦截特殊星星（偷星/毒星/毒液滩/收服） |
| `getAttackTarget` | 决定攻击目标时 | 多怪物场景选择目标 |
| `calculateDamageOverride` | 计算伤害时 | 使用 game.js 伤害公式覆盖 |
| `onStarHitMonster` | 星星命中怪物时 | 处理防御技能（吸收/闪避/护甲/护盾） |
| `onScoreEarned` | 得分时 | 后置效果（反弹/狂暴/毒液分裂/逃跑） |
| `onMonsterAttackHit` | 怪物攻击命中玩家时 | 模式特有伤害处理 |
| `onBattleStart` | 战斗开始时 | 初始化模式参数 |
| `onBattleUpdate` | 每帧更新时 | 模式特有逻辑 |
| `onBattleEnd` | 战斗结束时 | 结算处理 |

---

## 八、暂停对攻击的影响

当游戏暂停时（`PauseCoordinator`），`BattleEngine` 做以下补偿：

- **攻击间隔偏移**：所有 `attackers[].lastAttackTime += 暂停时长`
- **中毒时间偏移**：`poisonEndTime`、`poisonTickTime` += 暂停时长
- **眩晕时间偏移**：`stunEndTime` += 暂停时长
- **闪避时间偏移**：`dodgeEndTime` += 暂停时长
- **消散倒计时偏移**：`pendingDeaths[].deathTime` += 暂停时长
- **宠物攻击偏移**：`petLastAttackTime` += 暂停时长

恢复后不会出现怪物集中爆发攻击的情况。