# 器落山河 — 项目诊断报告

**报告时间**：2026-05-20  
**诊断方式**：只读扫描（未修改任何文件）  
**项目路径**：`E:\AIGame\V1`

---

## 一、项目概览

| 维度 | 数据 |
|------|------|
| 项目名称 | 器落山河 (qi-luo-shan-he) |
| 游戏类型 | Canvas 2D 回合制策略游戏 |
| 技术栈 | Rollup + IIFE 打包，Vanilla JS |
| 源文件数量 | 96 个 (.js)，分为 5 个子目录 |
| 代码量 | ~37,000 行（含 game.js 5,918 行） |
| 系统模块 | 42 个 (`src/systems/`) |
| 渲染器 | 19 个 (`src/renderers/`) |
| 测试文件 | 8 个测试套件 (`tests/`) |
| 构建产物 | `dist/game.js` (核心) + `dist/game-modules.js` (开发/测试) |
| 静态资源 | 47 张图片 + 13 个音频文件 |

### 目录结构

```
V1/
├── game.js              # 主入口，IIFE 包裹
├── src/
│   ├── entry.js         # 模块注册入口
│   ├── config/          # 配置层 (GameConfig, CombatSpec)
│   ├── systems/         # 42 个游戏系统模块
│   ├── renderers/       # 19 个渲染器
│   ├── state/           # 状态管理
│   ├── utils/           # 工具函数 (Logger, deepClone 等)
│   └── platform/        # 平台适配 (BrowserAPI)
├── tests/               # 测试套件
├── dist/                # 构建产物
└── assets/              # 图片 + 音频
```

### 架构评价

项目采用**闭包工厂 + 依赖注入**的架构风格，系统间通过 `entry.js` 统一注册和注入，解耦程度较好。核心亮点是 `CombatSpec` 的数值集中管理设计（支持强/弱校验、路径拼写 typo 检测），以及 `InvariantChecker` 的每帧运行时状态一致性校验。

**架构评分：8/10**

---

## 二、问题清单（按严重度排序）

### P0 — 阻塞级问题

#### 2.1 测试套件大面积崩溃

**影响范围**：`combat-spec.test.mjs`、`heal-star.test.mjs`、`bugfix-freeze.test.mjs`

**根因**：`src/platform/BrowserAPI.js` 在模块顶层直接执行了 DOM API 调用：

```javascript
document.addEventListener(...)     // 顶层，立即执行
_bindTouchEvents()                 // 顶层，立即执行
```

当测试套件在 Node.js 环境执行 `require('../dist/game-modules.js')` 时，模块加载阶段就触发 `ReferenceError: document is not defined`，三个测试套件全部崩溃。

**修复方案**：
- 方案 A（推荐）：将 DOM 相关初始化包裹进 `typeof document !== 'undefined'` 守卫，仅在浏览器环境执行
- 方案 B：将 DOM 绑定改为惰性初始化（首次调用时绑定）
- 方案 C：测试时注入 `global.document` 模拟对象

**注**：文件中存在 `var _isMocked = true` 变量，说明设计阶段已考虑过环境隔离，但未实际实现。

#### 2.2 测试套件未纳入 CI

目前测试需要手动执行 `node --test`，无 CI 配置，无法在每次提交时自动发现问题。

---

### P1 — 高风险问题

#### 2.3 调试日志在生产包泄漏

**位置**：

| 文件 | 行号 | 内容 |
|------|------|------|
| `game.js` | 第 5 行 | `const _isDev = true` |
| `src/utils/Logger.js` | 第 6 行 | `const isDev = true` |

**问题分析**：

- `Logger.js` 的 `isDev`：通过 Rollup 的 `build:prod` 配置可以做 tree-shake（条件编译），但需要确保 build 脚本确实处理了
- `game.js` 的 `_isDev`：其 `_log()` / `_warn()` 函数**完全绕过 Logger 系统**，在自己的 IIFE 作用域内独立判断，Rollup 的 tree-shake 无法触及

**结论**：即使执行 `build:prod`，`game.js` 中的 `_log/_warn` 调用仍然会在生产包中输出调试信息。

**修复方案**：
- 将 `_isDev` 替换为通过 Rollup `@rollup/plugin-replace` 注入的环境变量（如 `process.env.NODE_ENV`）
- 或者将 `game.js` 的日志调用统一迁移到 Logger 系统

#### 2.4 硬编码值可能影响多环境部署

除 `isDev` 外，`game.js` 中可能存在其他硬编码的环境相关常量，建议统一抽离到配置层。

---

### P2 — 中低风险问题

#### 2.5 开发包体积过大

| 文件 | 大小 | 问题 |
|------|------|------|
| `dist/game-modules.js` | 1.6 MB | 未压缩，测试和生产共用 |

**影响**：每次 `require` 加载 1.6MB 的 JavaScript，测试启动时内存占用和解析开销较大。

**修复方案**：为测试环境单独构建一个轻量版本（排除渲染器和平台适配层），或者使用压缩版本。

#### 2.6 game.js 存在残留空注释块

**位置**：`game.js` 第 65-87 行

这是系统模块迁移后的残骸，原先的功能已被拆到 `src/systems/` 下的各个模块中，但注释块留在了主文件里，增加了代码噪音。

**修复方案**：直接删除或替换为指向新位置的引用注释。

#### 2.7 音频资源重复

**位置**：`assets/audio/`

发现两个文件名带"副本"后缀的音频文件，疑似手动复制留下的冗余。

**修复方案**：确认两个文件内容一致后删除副本。

#### 2.8 缺少 Lint 和格式化配置

项目根目录未发现 `.eslintrc`、`.prettierrc` 或 `biome.json` 等配置文件，代码风格缺乏自动化保障。

---

## 三、测试状态总览

| 测试套件 | 文件 | 状态 | 用例数 |
|----------|------|------|--------|
| deepClone | `tests/utils/deepClone.test.mjs` | ✅ 通过 | 4/4 |
| fusion-core | `tests/fusion/fusion-core.test.mjs` | ✅ 通过 | 13/13 |
| upgrade-core | `tests/upgrade/upgrade-core.test.mjs` | ✅ 通过 | 30/30 |
| combat-spec | `tests/combat-spec.test.mjs` | ❌ 崩溃 | — |
| heal-star | `tests/heal-star.test.mjs` | ❌ 崩溃 | — |
| bugfix-freeze | `tests/bugfix-freeze.test.mjs` | ❌ 崩溃 | — |
| utils | `tests/utils/utils.test.mjs` | 未运行 | — |
| simulator | `tests/simulator/` | 未运行 | — |

**统计**：通过 47 个用例、3 个套件崩溃、2 个套件未运行。融合和升级系统的 43 个用例全部通过，核心逻辑质量可靠。

---

## 四、亮点总结

### 架构设计

1. **CombatSpec 数值系统**：战斗数值集中管理，支持强校验（阻断性错误）和弱校验（警告性提示），路径拼写错误还能通过 Levenshtein 距离给出修正建议——这在游戏项目中非常少见且价值极高。

2. **InvariantChecker 运行时校验**：每帧自动检查游戏状态一致性（如血量为负、阵亡单位仍可行动等），相当于给游戏装了一个内置 QA。

3. **依赖注入 + 闭包工厂**：系统模块间解耦清晰，新增系统时只需在 `entry.js` 注册即可。

### 代码质量

4. **模块拆分粒度合理**：42 个系统模块、19 个渲染器，职责清晰，没有出现单个文件数千行的巨型模块。

5. **`src/` 与 `game.js` 的双轨并存**：迁移策略正确——新代码在 `src/`，旧代码在 `game.js`，逐步替换而非一刀切。

---

## 五、修复优先级建议

```
优先级    问题                        预计工时
────────────────────────────────────────────
P0  ████  测试套件 DOM 崩溃修复        1-2h
P0  ████  补充 CI 配置                0.5-1h
P1  ███   生产包日志泄漏修复           0.5-1h
P1  ███   环境变量统一管理             1-2h
P2  ██    game-modules 压缩           0.5h
P2  ██    清理空注释块和重复资源       0.5h
P2  ██    添加 Lint 配置              1h
────────────────────────────────────────────
合计                                  5-8h
```

建议修复顺序：**P0 → P1 → P2**，先通测试、再堵日志、最后做优化。

---

## 六、技术债务雷达

| 债务项 | 当前影响 | 如果不修，3个月后 |
|--------|---------|-----------------|
| BrowserAPI DOM 裸调用 | 3 个测试套件不可用 | 新增系统无法自动测试，回归风险持续扩大 |
| isDev 硬编码 | 生产环境日志泄漏 | 性能下降 + 敏感信息暴露风险 |
| game-modules 1.6MB | 测试启动慢 | 测试套件越多越慢，开发者本地体验恶化 |
| 无 CI | 手动运行测试 | 合并代码前无法自动验证，Bug 率上升 |
| 无 Lint | 代码风格不统一 | 多人协作时 diff 污染严重 |

---

*报告结束。本报告通过只读扫描生成，未修改项目内任何文件。*
