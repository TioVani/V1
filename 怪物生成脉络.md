# 怪物生成脉络

## 一、整体架构

```
                    ┌─────────────────────────────┐
                    │      MonsterConfig.js        │
                    │  28种怪物定义 + Boss列表      │
                    │  稀有度权重 + 成长系数        │
                    └─────────────┬───────────────┘
                                  │ 选怪 & 属性计算
                                  ▼
┌─────────────────────────────────────────────────────┐
│               MonsterSpawnSystem.js                  │
│  spawnMonster / getRandomMonster / createMonster    │
│  checkMonsterAppear / calculateMonsterPositions     │
└───┬──────────┬──────────┬──────────┬────────────────┘
    │          │          │          │
    ▼          ▼          ▼          ▼
┌──────┐ ┌──────┐ ┌──────┐ ┌──────────┐
│ 普通  │ │ Boss │ │ 净化  │ │  大世界   │
│ 战斗  │ │ 战斗  │ │ 关卡  │ │  探索     │
└──────┘ └──────┘ └──────┘ └──────────┘
```

---

## 二、怪物定义层（MonsterConfig.js）

### 2.1 28种怪物 + Boss

| 稀有度 | 权重 | 怪物 |
|--------|------|------|
| UC | 60 | dust(灵尘)、wisp(微光灵絮)、shard(残片邪灵) |
| N | 50 | slime、bat、rat、spider、snake、boar |
| R | 30 | goblin、wolf、skeleton、zombie、harpy |
| SR | 15 | orc、ghost、golem、demon |
| SSR | 4 | necromancer、dragon、shadow_demon、fallenAngel |
| UR | 1 | ancient_dragon、void_creature |

每个怪物定义包含：id、名称、emoji图标、基础HP/攻击/防御、攻击间隔、技能列表、机制、掉落物、解锁分数。

**Boss列表**：slime_king → goblin_king → flame_lord → ice_queen → thunder_dragon → void_emperor → chaos_lord → abyss_lord → star_god → star_devourer → star_thief

### 2.2 成长公式

- HP = baseHp × 1.15^level
- 攻击 = baseAttack × 1.10^level
- 防御 = baseDefense × 1.05^level

### 2.3 选怪规则

- 按稀有度权重随机抽取
- 过滤掉 unlockScore > 当前分数的怪物
- 低分阶段有额外阶梯限制（getMonsterTypeByScore）

---

## 三、生成中枢（MonsterSpawnSystem.js）

这是所有怪物生成的核心入口，以闭包工厂模式组织。

### 3.1 主流程：spawnMonster(type, options)

```
spawnMonster("slime" 或 "boss", {level: 1})
    │
    ├─ 选怪：getRandomMonster(score) 或 getBossTypeByScore(score)
    │
    ├─ 计算属性：getMonsterInstance(monsterId, level)
    │    └─ 应用成长公式 + 增强加成(score≥2000时HP翻倍)
    │
    ├─ 创建对象：createMonster(type, x, y, options)
    │    ├─ 基础属性：id/hp/maxHp/attack/defense
    │    ├─ 战斗字段：shield/armor/skills/skillStates
    │    ├─ 特殊状态：hasRaged/hasSplit/empowered
    │    ├─ 觉醒能力：awakeDodge/awakeArmor/awakeResist/awakeRage
    │    └─ 吸收机制：absorbType (score≥2000后启用)
    │
    └─ 注入数组：monsters.push(monster)
```

### 3.2 多怪物位置计算

| 数量 | 排列方式 |
|------|----------|
| 1个 | 居中 (x=400, y=350) |
| 2个 | 各偏100px (x=300, x=500) |
| 3个 | 三角排列 |
| 4+ | 线性排列，间距85px |

### 3.3 自动出怪检查（checkMonsterAppear）

在普通/季节模式下持续轮询：
- 条件1：当前没有活跃怪物（activeMonsters为空）
- 条件2：分数 ≥ 60（灵辉值达标）
- 条件3：击杀数达到该分数段的阈值
- 满足后自动调用 spawnMonster()

---

## 四、五种生成场景

### 4.1 普通战斗

**入口**：GameLifecycleSystem → MonsterSpawnSystem.spawnMonster()

**流程**：
```
开局 → 检查历史最高分数
    ├─ bestScore ≥ 60 → 直接 spawnMonster('slime')
    └─ bestScore < 60 → 等待教学/新手期
         │
循环 → checkMonsterAppear() 每帧检测
    ├─ 分数达标 + 无活跃怪物 + 击杀数达标 → 生成新怪物
    └─ 不达标 → 跳过

怪物死亡 → activeMonsters长度-1 → 下一帧继续检测
```

**特殊规则**：
- 5%概率将Boss替换为star_thief（偷星者）
- score≥2000后怪物HP翻倍（普通×1.5）
- 觉醒阶段会缩放怪物HP和攻击力

### 4.2 Boss战斗

**入口**：BossBattleAdapter.init() → 直接创建Boss放入monsters[]

**Boss选角逻辑**：
```
getBossTypeByScore(score)
    ├─ star_devourer：15-20%概率独占（特定分数段）
    ├─ void_emperor：5%概率
    ├─ star_god：最高层Boss
    └─ 其他按分数阶梯递进（slime_king→goblin_king→...）
```

**Boss技能触发**：
- 分裂技能 → 生成额外小怪物对象
- 吸收技能 → 怪物获得absorbType和empowered状态

### 4.3 净化关卡（StageConfig.js）

**入口**：StageModeSystem

**配置结构**（共9关 stage_1_1 ~ stage_3_3）：
```
关卡N：
  monsterTypes: ['slime', 'bat', 'rat']     ← 可出的小怪类型
  monsterCount: 5                            ← 小怪数量
  bossType: 'slime_king'                     ← Boss类型
  hpMultiplier: 1.0                          ← HP倍率
```

**流程**：
```
进入关卡 → spawnNextMonster() 逐只出小怪
    │
打完monsterCount只 → spawnBoss() 出Boss
    │
击败Boss → 关卡通关
```

### 4.4 大世界探索（WorldMapConfig.js）

**配置结构**：16个世界，每个世界有固定的enemy实体列表

```
world_01:
  entities:
    - {type: 'enemy', id: 'enemy_01', x: 634, y: 154,
       monster: 'slime', level: 1,
       discoverRadius: 80, interactRadius: 50,
       once: false, respawnTime: 0}
```

**交互流程**：
```
玩家移动 → 进入discoverRadius → 实体被发现
    │
玩家靠近 interactRadius → WorldMapEntity.interact('enemy')
    │
返回 {monster: 'slime', level: 1} → 外部发起战斗
```

**刷新规则**：
- `once: false` + 玩家离开世界 → 实体恢复未解决状态
- `respawnTime: 0` → 离开时立即刷新
- `respawnTime: N` → 离开后N毫秒刷新

### 4.5 塔模式（TowerSystem.js）

**流程**：
```
每层网格按概率放置怪物：
    ├─ normal：普通怪物（概率由配置决定）
    ├─ elite：精英怪物
    └─ boss：Boss怪物

玩家交互 → 返回怪物ID和等级 → 进入战斗
```

---

## 五、怪物生命周期

```
创建（spawnMonster/createMonster）
    │
    ▼
活跃（active=true，参与战斗）
    │
    ├─ 技能触发 → 分裂 → 生成新怪物对象（子怪物链接原Boss）
    │
    ▼
死亡（HP归零）
    │
    ├─ 掉落计算 → 金币/经验/材料/收藏品
    ├─ 图鉴解锁 → 怪物ID写入图鉴数据库
    ├─ 收服判定 → 概率触发收服（收服系统）
    └─ 从monsters数组移除 → activeMonsters--
```

---

## 六、关键阈值速查

| 条件 | 阈值 | 效果 |
|------|------|------|
| 首次出怪 | score ≥ 60 | 灵辉值达标，怪物开始出现 |
| 自动出怪检查 | activeMonsters = 0 + 击杀数达标 | 每帧检测是否该出下一只 |
| 怪物增强 | score ≥ 2000 | 怪物HP翻倍，开启吸收能力 |
| 偷星者替换 | 5%概率 | Boss被替换为star_thief |
| 吞噬Boss独占 | 15-20%概率 | star_devourer替换当前分数段Boss |

---

## 七、拓展节点

- **战斗适配器层**：NormalBattleAdapter / BossBattleAdapter 各自处理怪物死亡、分裂、状态流转
- **技能系统**：MonsterSkillSystem 管理13种怪物技能的触发和效果
- **渲染层**：MonsterDrawRenderer 绘制怪物emoji、血条、护盾、稀有度标记、吸收标签
- **图鉴系统**：怪物被击杀后写入图鉴数据库
- **收服系统**：怪物死亡时有概率触发收服流程