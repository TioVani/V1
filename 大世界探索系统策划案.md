# 大世界探索系统策划案

## 一、完整状态流

```
title              启动画面，点击"开始游戏"
  ↓
loading            资源预加载（按需，资源已缓存时自动跳过）
  ↓
cutscene           开场过场动画，自动播放
  ↓
worldmap           玩家出现在起始区域，地图受限，屏幕显示引导指示（箭头/光标）指向两只古灵争斗方向
  ↓ (走到两个古灵打架处，按E/点击触发)
tutorial           教学战斗（复用 BattleEngine）
  ├─ (胜利) → worldmap（地图开放）
  └─ (失败) → tutorial（自动重新开始教学战斗，无惩罚、无次数限制）
  ↓
worldmap           古灵解锁菜单栏能力，地图开放
  ↓
自由探索：
  - 走到宝箱 → 按E/点击 → 开启获得奖励
  - 走到对手 → 按E/点击 → 进入战斗 → 返回触发点原位
  - 走到无尽之塔 → 按E/点击 → 进入爬塔 → 同时解锁菜单栏"无尽之塔"按钮 → 返回塔入口位置
  - 探索度达标 → 传送至下一个大世界（如 World 02）
  - World 02 被冥河拦住 → 与摆渡人对话获得谜语线索 → 通关该世界无尽之塔 → 摆渡人渡河，彼岸开放
```

### 1.1 子状态返回 worldmap 的位置规则

| 来源状态 | 返回位置 | 说明 |
|---|---|---|
| tutorial（胜利） | 触发教学的实体位置 | 地图开放 |
| tutorial（失败） | 不返回 worldmap | 自动重新开始教学战斗，循环直到胜利 |
| boss_select / stage_select | 触发战斗的对手实体位置 | 胜利和失败均返回触发实体位置。失败后可重新交互再次挑战，无惩罚 |
| enemy（普通敌人战斗） | 触发战斗的对手实体位置 | 胜利和失败均返回触发实体位置。失败后实体不刷新（`resolved` 不置 `true`），玩家可重新交互再次挑战 |
| tower | 塔入口实体位置 | 爬塔退出/结算后 |
| shop | 进入商店前的玩家位置 | 商店不改变地图位置 |
| 其他子状态 | 进入前的玩家位置（`returnPosition` 快照） | 通用规则 |

> **机制**：每次从 worldmap 离开时，`WorldMapSystem` 自动快照 `returnPosition = getPlayerPos()`；返回 worldmap 时调用 `setPlayerPos(returnPosition)` 恢复。

### 1.2 多大世界衔接规则

```
world_01（灵域初境） → portal(world_02) → world_02（冥河幽境·埃及）
```

- 进入新世界时：**无 cutscene**，直接进入 worldmap（后续可通过 WorldMapConfig 配置 `entryCutscene` 字段按需插入）
- 新世界的 tutorial：由 `WorldMapConfig.hasTutorial` 控制，为 `true` 时该世界有独立教学流
- 旧世界可回传：portal 实体为单向传送（`targetWorld` 指定目标世界），回传时自动在目标世界中查找 `targetWorld` 等于当前世界的 portal 坐标作为落点
- **回传位置规则**：从 world_02 回到 world_01 时，系统在 world_01 的实体列表中查找 `targetWorld: 'world_02'` 的 portal 实体，玩家出现在该 portal 的坐标位置（即出发时走的那个传送门）
- 每个世界的进度独立存档，互不干扰
- **首次进入 vs 回到已访问世界**：`loadWorld(worldId)` 加载时，若 `worldMapProgress.worlds[worldId]` 存在（已访问过），则从存档的 `playerPos` 恢复位置；若不存在（首次进入），则使用 WorldMapConfig 的 `playerStart`

### 1.3 冥河屏障机制（world_02 及后续世界）

部分世界存在**天然屏障**（如 world_02 的冥河），将地图分为"岸上区域"和"彼岸区域"。玩家通过传送门到达该世界后只能在岸上区域活动，无法通过冥河。

**解锁流程：**

1. 玩家到达 World 02，发现冥河阻挡去路
2. 岸上有**摆渡人 NPC**，交互后获得谜语线索，暗示需要通关无尽之塔
3. 通关该世界的无尽之塔 → 摆渡人认可玩家实力，渡玩家过冥河

通关无尽之塔后：
- 冥河屏障实体（`type: 'barrier'`）的 `resolved` 置 `true`，碰撞层移除冥河阻挡
- 彼岸区域的实体（宝箱/对手/传送门等）变为可发现

> **当前 demo 阶段**：World 02 岸上不放置宝箱/敌人/塔，彼岸也不配置实体。玩家只能在岸边观看冥河对岸、与摆渡人对话、通过传送门返回 World 01。摆渡人的谜语和冥河屏障机制已完整定义，待正式制作时补充实体即可激活完整流程。

> **设计意图**：探索度决定能否到达新世界，无尽之塔决定能否在新世界中继续深入。摆渡人的谜语为玩家提供方向引导，避免卡关。

## 二、UI 层级

```
┌─────────────────────────────────────┐
│  世界地图背景（摄像机跟随角色）        │
│                                     │
│   📦宝箱   ⚔对手   🏔古灵打架处      │
│        🔵 玩家角色                   │
│              🗼无尽之塔入口           │
│   🌀传送点(探索度达标后激活)          │
│                                     │
│  [齿轮] [任务]                       │
│  [菜单☰]          [角色][编队]       │
└─────────────────────────────────────┘
```

- **底层**：世界地图 Canvas
- **中层**：玩家角色 + 地图实体（宝箱/对手/NPC/塔/传送点）
- **顶层**：左上角设置/任务图标、左下角菜单栏按钮、右下角角色/编队图标 — 位置和样式与现有一致

## 三、教学战斗前后的变化

| 元素 | 教学前 | 教学后 |
|---|---|---|
| 地图可见范围 | 起始小区域 + 古灵打架处 | 大幅开放 |
| 引导指示 | 屏幕显示箭头/光标指向 tutorial 实体方向，玩家进入交互范围后消失 | 无（已移除） |
| 左下角菜单栏 | 隐藏 | 显示（但部分按钮灰锁） |
| 宝箱/对手 | 不存在 | 出现 |
| 无尽之塔 | 不存在 | 地图上可见塔的实体 |
| 传送点 | 不存在 | 探索度达标后出现 |

> **教学前地图受限的实现**：教学前的活动范围限制通过 WorldMapConfig 中的碰撞层实现，而非摄像机裁剪。`requireTutorial: true` 的实体在教学完成前不激活（不可见、不可交互），配合碰撞边界将玩家限制在起始区域 + tutorial 实体附近。教学完成后，`onTutorialWin()` 移除教学专用碰撞边界，同时激活所有 `requireTutorial: true` 的实体。不需要额外的摄像机限制逻辑。

> **教学前引导指示**：教学未完成时，`WorldMapRenderer` 在屏幕上绘制一个指向 tutorial 实体的引导指示（箭头/光标），帮助玩家快速找到两只古灵争斗的位置。引导逻辑：从玩家当前位置到 tutorial 实体坐标的方向向量 → 在屏幕边缘或角色附近渲染箭头。当玩家进入 tutorial 实体的 `interactRadius` 时引导消失。引导由 `WorldMapConfig.hasTutorial` 和 `tutorialComplete` 状态共同控制，仅在教学未完成的世界中显示。

> **失败处理**：教学战斗失败后自动重新开始，不返回世界地图，玩家无需重新走到古灵打架处。循环直到胜利。无惩罚、无次数限制。

## 四、交互方式

所有地图实体统一为**玩家主动交互**，不自动触发：

1. 走到实体**发现范围**（`discoverRadius`）内 → 实体在地图上可见（计入探索度）
2. 走到实体**交互范围**（`interactRadius`）内 → 出现交互提示（高亮/文字）
3. 按 E 键 / 点击实体 → 触发交互

> `discoverRadius` ≥ `interactRadius`，默认 `discoverRadius = interactRadius × 2`。玩家可以先"发现"一个实体但还没走到交互位置。

### 4.1 多实体同时在交互范围内的优先级

当多个实体同时满足交互条件时，按以下规则排序：

1. **按类型权重**：`tutorial(100) > portal(90) > barrier(85) > tower(80) > enemy(60) > chest(40) > npc(20)`
2. **同类型按距离**：距离玩家中心更近的优先
3. 最终 `getNearbyEntity()` 返回优先级最高的**一个**实体

### 4.2 触屏交互防误触

- 虚拟摇杆控制移动，点击实体区域弹出交互按钮（"开启"/"挑战"/"进入"等）
- 二次确认后才触发交互，避免摇杆操作时误触

### 4.3 操作对照

| 操作 | 键盘 | 触屏 |
|---|---|---|
| 移动 | WASD / 方向键 | 虚拟摇杆（左下角，菜单按钮上方） |
| 交互 | E 键 | 点击实体 → 弹出确认按钮 → 点击确认 |

## 五、菜单栏解锁机制

菜单栏按钮由两种方式解锁：

1. **地图探索解锁**：走到对应实体并交互
   - 走到无尽之塔 → 交互完成后解锁菜单栏"无尽之塔"按钮（以后可快捷进入）

2. **进度解锁**（保留现有逻辑）
   - `isModeUnlocked()` 检查 `bestScore`、`playerData` 等条件
   - Boss战、赛季等仍按分数/进度解锁

两种条件**同时满足**时按钮才完全可用；满足进度但未在地图发现时显示灰锁提示"前往大世界解锁"。

## 六、探索度系统

### 6.1 计算方式

以**实体（entity）为最小单位**，每个实体有两种探索状态：

```js
entityState = {
    discovered: false,   // 是否已发现（进入 discoverRadius）
    resolved: false,     // 是否已处理（开箱/击败/交互完成）
}
```

- **探索度 = 已 discovered 的实体数 / 当前大世界活跃实体总数**
- **完成度 = 已 resolved 的实体数 / 当前大世界活跃实体总数**
- **活跃实体总数**：当前世界中满足所有前置条件的实体数量。`requireTutorial: true` 的实体在教学未完成时不计入总数；被 barrier 的 `hiddenEntityIds` 隐藏的实体不计入总数。教学完成后或 barrier 解除后，新激活的实体加入总数。因此活跃实体总数可能随游戏进度增长，但探索度百分比不会因此下降（已 discovered 的实体不会因总数增长而取消发现）
- 传送点激活条件：探索度 ≥ 阈值（如 80%）
- 完成度仅用于展示/成就，不影响核心流程

### 6.2 数据结构

```js
worldMapProgress = {
    currentWorldId: 'world_01',
    cutsceneWatched: false,              // 全局标记：开场过场动画是否已播放过（true 则后续可跳过）
    worlds: {
        'world_01': {
            playerPos: { x: 100, y: 800 },
            returnPosition: null,         // 离开 worldmap 时的快照
            entities: {
                'tutorial_spirits': { discovered: true, resolved: true },
                'chest_01': { discovered: true, resolved: false },
                'enemy_01': { discovered: false, resolved: false },
            },
            tutorialComplete: true,
            unlockedMenus: ['tower'],
        },
        'world_02': { ... },
    },
}
```

> **`cutsceneWatched` 说明**：该字段为全局标记（非 per-world），放在 `worldMapProgress` 顶层。首次播放过场时为 `false`（不可跳过），播放完毕后置 `true` 并即时存档。后续重播均可跳过。

### 6.3 存档策略

- **即时存档**：每次 `interact()` 成功后、离开 worldmap 状态时、跨世界传送时，立即写入 `playerData`。`playerPos` 在每次 `movePlayer()` 调用后更新内存中的值，但在上述时机才持久化到存档（避免每帧写存档）
- **按 worldId 分区**：每个世界的进度互不干扰
- **不支持回滚**：进度只增不减，不提供重置单个世界功能（避免刷奖励）
- **可重复实体（`once: false`）的 resolved 状态**：`resolved` 标记在交互产生实质性结果时置 `true`（如战斗胜利、宝箱开启），实体刷新后重置为 `false`；但 `discovered` 一旦为 `true` 永不重置。因此探索度只增不减，完成度可能因刷新而波动（仅影响展示，不影响核心流程）。**例外：NPC 和 portal 不标记 `resolved`**，因为它们是纯对话/传送，无消耗性结果
- 跨世界数据（如货币、角色养成）仍由现有 `playerData` 管理，不由 `worldMapProgress` 管理

## 七、模块划分

### 7.1 顶层模块

| 模块 | 文件 | 职责 |
|---|---|---|
| TitleRenderer | `src/renderers/TitleRenderer.js` | 启动画面渲染、点击检测 |
| CutsceneRenderer | `src/renderers/CutsceneRenderer.js` | 过场动画帧序列、自动播放结束 |
| WorldMapRenderer | `src/renderers/WorldMapRenderer.js` | 地图绘制、角色渲染、摄像机跟随、实体绘制、教学前引导箭头渲染 |
| WorldMapConfig | `src/config/WorldMapConfig.js` | 每个大世界的地图数据（区域、节点、碰撞层等） |

### 7.2 WorldMapSystem 子模块拆分

WorldMapSystem 拆为 1 个入口 + 4 个子模块，遵循项目已有的 `src/systems/fusion/`、`src/systems/upgrade/` 子目录模式：

```
src/systems/
  WorldMapSystem.js              # 入口，组合子模块，对外暴露统一接口
  worldmap/
    WorldMapPlayer.js            # 角色移动 + 碰撞检测
    WorldMapEntity.js            # 实体管理（发现/交互分发/优先级/刷新）
    WorldMapExploration.js       # 探索度计算 + 存档读写
    WorldMapUnlock.js            # 菜单解锁判定 + 屏障解除
```

| 子模块 | 职责 | 暴露接口 |
|---|---|---|
| WorldMapPlayer | 角色坐标管理、移动输入处理、碰撞检测（rect/tilemap）、边界约束 | `movePlayer`, `getPlayerPos`, `setPlayerPos`, `snapshotReturnPosition`, `restoreReturnPosition`, `checkCollision` |
| WorldMapEntity | 实体生命周期（发现/交互/解决/刷新）、多实体优先级排序、按 type 分发交互 | `getEntities`, `getNearbyEntity`, `getDiscoveredEntities`, `interact`, `isEntityDiscovered`, `isEntityResolved` |
| WorldMapExploration | 探索度/完成度计算、worldMapProgress 存档读写、跨世界进度管理 | `loadWorld`, `getWorldId`, `getExplorationPercent`, `getCompletionPercent`, `saveProgress` |
| WorldMapUnlock | 菜单栏解锁状态管理、barrier 屏障条件判定与解除 | `isMenuUnlocked`, `unlockMenu`, `checkBarriers`, `onTutorialWin`, `onTutorialLose`, `isTutorialComplete` |

WorldMapSystem.js 作为入口，在构造时实例化四个子模块并组合，对外暴露统一接口（第八节），调用方无需感知子模块存在。

## 八、WorldMapSystem 核心接口

WorldMapSystem 对外暴露统一接口，内部委托给子模块处理：

```js
createWorldMapSystem(deps)

// === 地图管理 → 委托 WorldMapExploration ===
.loadWorld(worldId)              // 加载某个大世界
.getWorldId()                    // 当前大世界 ID
.getExplorationPercent()         // 当前探索度（discovered / total）
.getCompletionPercent()          // 当前完成度（resolved / total）

// === 角色移动 → 委托 WorldMapPlayer ===
.movePlayer(dx, dy, dt)          // 移动玩家（键盘/摇杆输入），内部处理碰撞
.getPlayerPos()                  // 玩家当前位置
.setPlayerPos(x, y)             // 设置玩家位置（传送等）
.snapshotReturnPosition()        // 快照当前位置为 returnPosition
.restoreReturnPosition()         // 从 returnPosition 恢复位置
.checkCollision(x, y)            // 检查目标位置是否可通行

// === 实体交互 → 委托 WorldMapEntity ===
.getEntities()                   // 获取所有当前活跃的地图实体（已过滤未激活的：requireTutorial 未满足、barrier hiddenEntityIds 中的实体不返回）
.getNearbyEntity()               // 获取当前交互范围内优先级最高的活跃实体
.getDiscoveredEntities()         // 获取所有已发现且活跃的实体（用于渲染，未激活实体不参与）
.interact(entityId)              // 主动交互（开宝箱/进入战斗/进塔/对话），按 type 分发
.isEntityDiscovered(id)          // 实体是否已发现
.isEntityResolved(id)            // 实体是否已处理

// === 解锁与教学 → 委托 WorldMapUnlock ===
.isMenuUnlocked(menuId)          // 某菜单项是否已解锁
.unlockMenu(menuId)              // 解锁菜单项
.checkBarriers()                 // 检查所有 barrier 实体，满足条件时自动解除
.isTutorialComplete()            // 教学是否完成
.onTutorialWin()                 // 教学胜利回调（开放地图、解锁菜单）
.onTutorialLose()                // 教学失败回调（自动重开战斗，不返回 worldmap）
```

## 九、WorldMapConfig 数据结构

```js
{
    worldId: 'world_01',
    name: '灵域初境',
    width: 2000, height: 1500,     // 地图尺寸（像素）
    playerStart: { x: 100, y: 800 },
    hasTutorial: true,             // 是否有教学流程
    entryCutscene: null,           // 进入该世界时的过场 ID，null 则跳过

    // 碰撞层：定义不可通行区域
    collisions: [
        { type: 'rect', x: 300, y: 200, w: 100, h: 50 },
        { type: 'rect', x: 700, y: 500, w: 200, h: 30 },
        // 更多矩形阻挡区...
    ],
    // 边界碰撞默认开启（角色不可超出 width/height）

    entities: [
        {
            id: 'tutorial_spirits',
            type: 'tutorial',
            x: 400, y: 800,
            discoverRadius: 120,      // 发现范围（默认 interactRadius × 2）
            interactRadius: 60,       // 交互范围
            priority: 100,            // 交互优先级权重
            once: true,               // 是否一次性（完成后消失/不可再触发）
        },
        {
            id: 'chest_01',
            type: 'chest',
            x: 600, y: 400,
            discoverRadius: 60,
            interactRadius: 30,
            priority: 40,
            once: true,
            requireTutorial: true,     // 教学完成后才出现
            reward: { currency: 100, items: ['potion_small'] },
        },
        {
            id: 'enemy_01',
            type: 'enemy',
            x: 900, y: 300,
            discoverRadius: 80,
            interactRadius: 40,
            priority: 60,
            once: false,              // 可重复挑战
            requireTutorial: true,     // 教学完成后才出现
            respawnTime: 0,           // 0 = 离开世界地图后刷新，单位秒
            monster: 'slime',
            level: 1,
        },
        {
            id: 'tower',
            type: 'tower',
            x: 1500, y: 200,
            discoverRadius: 160,
            interactRadius: 80,
            priority: 80,
            once: false,
            requireTutorial: true,     // 教学完成后才出现
            unlockMenuId: 'tower',    // 交互后解锁的菜单项
        },
        {
            id: 'portal',
            type: 'portal',
            x: 1800, y: 800,
            discoverRadius: 100,
            interactRadius: 50,
            priority: 90,
            once: false,
            requireTutorial: true,     // 教学完成后才出现
            targetWorld: 'world_02',  // 传送到哪个世界
            requireExploration: 0.8,  // 需要当前世界探索度 ≥ 80%
            lockedMessage: '探索度未达标，无法传送', // 探索度不足时显示的提示文案
        },
    ],
}
```

### 9.1 实体类型通用字段说明

| 字段 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `id` | string | 必填 | 实体唯一标识 |
| `type` | string | 必填 | `tutorial/chest/enemy/tower/portal/barrier/npc` |
| `x`, `y` | number | 必填 | 实体中心坐标 |
| `interactRadius` | number | 40 | 交互触发半径 |
| `discoverRadius` | number | interactRadius × 2 | 发现半径，进入此范围实体变为可见 |
| `priority` | number | 见 4.1 | 多实体重叠时的交互优先级 |
| `once` | boolean | false | 是否一次性（宝箱/教学为 true） |
| `respawnTime` | number | 0 | 非 once 实体的重生时间（秒），0 = 离开世界地图后刷新 |
| `requireTutorial` | boolean | false | 是否需要教学完成后才激活。为 `true` 时，教学未完成前该实体不可见、不可交互、不计入探索度 |

### 9.1a 各实体类型专有字段

#### chest（宝箱）

| 字段 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `reward` | object | `{}` | 奖励内容，如 `{ currency: 100, items: ['potion_small'] }` |

> **交互行为**：触发后播放开启动画 → 发放 `reward` → 实体标记 `resolved`。`once: true` 时开过后不可再开。

#### enemy（对手）

| 字段 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `monster` | string | 必填 | 怪物标识，对应 BattleEngine 中的怪物配置 |
| `level` | number | 1 | 怪物等级 |

> **交互行为**：触发后进入战斗（复用 BattleEngine）→ 战斗结束返回触发实体位置。**胜利**时 `resolved` 置 `true`，`once: false` 的敌人刷新后重置为 `false` 可再次挑战。**失败**时同样返回触发实体位置，`resolved` 不变（保持 `false`），玩家可重新交互再次挑战，无惩罚、无次数限制。

#### tower（无尽之塔）

| 字段 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `unlockMenuId` | string | null | 交互后解锁的菜单项 ID，如 `'tower'` |

> **交互行为**：触发后进入爬塔流程 → 同时解锁 `unlockMenuId` 对应的菜单栏按钮（如菜单栏"无尽之塔"快捷入口）→ 退出/结算后返回塔入口实体位置。

#### portal（传送门）

| 字段 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `targetWorld` | string | 必填 | 传送目标世界的 worldId |
| `requireExploration` | number | 0 | 当前世界探索度阈值（0 = 无条件） |
| `lockedMessage` | string | '' | 条件未满足时的提示文案 |

> **交互行为**：检查 `requireExploration`，未达标则按 9.2 规则显示 `lockedMessage`；达标则传送至 `targetWorld`，落点为该世界中 `targetWorld` 等于当前世界 ID 的 portal 坐标。portal 不标记 `resolved`（可反复传送）。

#### npc（NPC）

| 字段 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `dialogue` | string[] | [] | 对话内容数组，每项为一条台词 |

> **NPC 交互行为**：
> - 触发交互后弹出对话框，逐条显示 `dialogue` 数组中的台词，玩家点击/按 E 切换下一条
> - 所有台词播放完毕后，对话框自动关闭，交互结束
> - NPC 不标记 `resolved`（`once` 默认 `false`），可反复对话，每次交互完整播放所有台词
> - 对话期间玩家不可移动（与战斗/爬塔相同的输入锁定机制）

### 9.2 条件未满足时的交互行为

当实体有前置条件但条件未满足时（如传送门 `requireExploration` 不达标），玩家仍可交互，但系统给出明确反馈：

- 走到交互范围内 → 出现交互提示（与正常实体一致）
- 触发交互 → 弹出提示框显示 `lockedMessage`，不执行实际逻辑
- 提示框同时显示当前进度（如"当前探索度 65%，需要 80%"）
- 玩家确认后关闭提示框，继续自由移动

### 9.3 barrier 实体（天然屏障）特有字段

`barrier` 类型用于表示阻挡玩家通行的天然屏障（如冥河、深渊等），需要满足特定条件后才会解除。

| 字段 | 类型 | 默认值 | 说明 |
|---|---|---|---|
| `requireTowerClear` | boolean | false | 是否需要通关该世界的无尽之塔才能解除屏障 |
| `requireExploration` | number | 0 | 需要的探索度阈值（0 = 不要求） |
| `collisionRefs` | string[] | [] | 关联的碰撞区域 ID 列表，屏障解除后这些碰撞区域从 collisions 中移除 |
| `hiddenEntityIds` | string[] | [] | 屏障解除后才允许发现的实体 ID 列表（彼岸区域的实体） |
| `unlockMessage` | string | '' | 屏障解除时显示的提示文案（如"摆渡人认可你的实力，渡你过冥河"） |

> **barrier 解除判定**：`WorldMapSystem` 在每次探索度变化或无尽之塔通关后检查所有 `type: 'barrier'` 的实体，当其全部条件满足时自动 resolve（更新碰撞层、解锁隐藏实体）。玩家走到 barrier 实体处交互时，若条件未满足则显示进度提示（同 9.2 规则）。

### 9.4 World 02 配置示例（冥河幽境·埃及）

```js
{
    worldId: 'world_02',
    name: '冥河幽境·埃及',
    width: 2400, height: 1800,
    playerStart: { x: 100, y: 900 },
    hasTutorial: false,            // 第二个世界无教学
    entryCutscene: null,

    collisions: [
        { id: 'styx_river', type: 'rect', x: 1000, y: 0, w: 120, h: 1800 },  // 冥河，纵向贯穿地图
        // 更多碰撞区...
    ],

    // NOTE: World 02 当前为 demo 展示阶段，玩家只能在岸边观看彼岸、与摆渡人对话。
    // 岸上不放置宝箱/敌人/塔，彼岸实体暂不配置。
    // 后续正式制作时，在此处补充完整实体列表（chest/enemy/tower/portal 等）。
    entities: [
        // === 返回 World 01 的传送门 ===
        {
            id: 'portal_return',
            type: 'portal',
            x: 200, y: 900,             // 岸边起始区域
            discoverRadius: 100,
            interactRadius: 50,
            priority: 90,
            once: false,
            targetWorld: 'world_01',     // 返回灵域初境
            requireExploration: 0,       // 无条件，随时可返回
        },
        // === 摆渡人 NPC ===
        {
            id: 'ferryman',
            type: 'npc',
            x: 950, y: 850,             // 冥河岸边
            discoverRadius: 80,
            interactRadius: 40,
            priority: 30,
            once: false,
            dialogue: [
                '生者啊，冥河的对岸有着不为人知的秘密。',
                '想要过河？证明你的实力吧——征服那座试炼之塔，我便渡你过去。',
                '古灵们留下的试炼之塔就在北面，那是通往彼岸的唯一途径。',
            ],
        },
        // === 冥河屏障 ===
        {
            id: 'styx_barrier',
            type: 'barrier',
            x: 1060, y: 900,             // 冥河中央位置
            discoverRadius: 200,
            interactRadius: 60,
            priority: 85,
            once: true,
            requireTowerClear: true,     // 唯一条件：通关无尽之塔
            collisionRefs: ['styx_river'],  // 解除后移除冥河碰撞
            hiddenEntityIds: [],         // demo 阶段彼岸无实体
            lockedMessage: '冥河波涛汹涌，无法通行。或许摆渡人知道方法……',
            unlockMessage: '摆渡人认可你的实力，渡你过冥河！',
        },
    ],
}
```

## 十、StateMachine 新增

```js
// 新增状态
'title', 'loading', 'cutscene', 'worldmap', 'tutorial'

// 转移规则
title     → loading
loading   → cutscene（资源就绪后自动跳转；若已缓存则 title → cutscene 直通）
cutscene  → worldmap（播放完毕自动跳转；点击任意位置 / 按 ESC 跳过；首次播放不可跳过，判断依据：`worldMapProgress.cutsceneWatched === false`，首次播放时 `CutsceneRenderer` 的 `skippable` 置 `false`，播放完毕后将 `cutsceneWatched` 置 `true` 并即时存档，后续重播均可跳过。注：开场过场发生在世界加载之前，不能用 per-world 数据判断）
worldmap  → tutorial, boss_select, stage_select, tower, shop, ...
tutorial  → worldmap（胜利）；tutorial（失败，自动重开）

// loading 状态
// - 检查目标状态所需的资源是否已加载
// - 已加载：自动跳转到下一状态
// - 未加载：显示加载画面，加载完成后跳转
// - 可配置超时和降级策略
```

`menu` 状态废弃，所有原来回到 `menu` 的路径改为回到 `worldmap`。

### 10.1 menu → worldmap 迁移清单

需全量搜索以下跳转点并逐一替换：

| 原路径 | 新路径 | 备注 |
|---|---|---|
| 战斗结束 → menu | 战斗结束 → worldmap | `returnPosition` 恢复位置 |
| 塔结算 → menu | 塔结算 → worldmap | 回到塔入口 |
| 商店退出 → menu | 商店退出 → worldmap | 回到进入商店前位置 |
| 设置返回 → menu | 设置返回 → worldmap | 位置不变 |
| 初始启动 → menu | 初始启动 → title | 全新入口 |
| 强制返回主页按钮 → menu | 强制返回主页按钮 → worldmap | 保留该按钮功能 |

## 十一、对现有代码的影响范围

| 文件 | 改动 |
|---|---|
| `game.js` | 初始状态改 `title`；渲染分发加 title/cutscene/worldmap/tutorial 四个分支 + loading 分支；`menu` 触摸逻辑迁移到 `worldmap`；所有 `→ menu` 替换为 `→ worldmap` |
| `StateMachine.js` | 加 5 个状态（title/loading/cutscene/worldmap/tutorial）+ 转移规则 |
| `MenuRenderer.js` | 精简：保留左下角菜单栏和角落图标，去掉踏入灵域/闯关按钮 |
| `GameLifecycleSystem.js` | 不改 |
| `BattleEngine.js` | 不改（教学战斗复用现有逻辑） |
| `TowerSystem.js` | 不改 |
| `entry.js` | 加 9 个新模块的 import + 导出（TitleRenderer、CutsceneRenderer、WorldMapSystem、WorldMapPlayer、WorldMapEntity、WorldMapExploration、WorldMapUnlock、WorldMapRenderer、WorldMapConfig） |
| 其余所有文件 | 不改 |

## 十二、开发顺序

```
Phase 1: TitleRenderer + CutsceneRenderer + Loading 逻辑（启动画面 + 资源加载 + 过场）
Phase 2: WorldMapPlayer + WorldMapRenderer（角色移动 + 碰撞 + 摄像机跟随）
Phase 3: WorldMapEntity + WorldMapUnlock（实体交互框架 + 教学战斗 + 菜单解锁）
         - 实现 discoverRadius / interactRadius 双范围
         - 实现优先级排序
         - 实现 interact() 分发（按 type 走不同分支）
         - tutorial 作为 type='tutorial' 的特殊实体，胜利/失败回调走此 Phase
Phase 4: WorldMapExploration + 多大世界传送（探索度 + 存档 + portal/barrier）
```

> **Phase 3 合并说明**：教学战斗本质上是 `type: 'tutorial'` 的实体交互，不需要单独一个 Phase。统一交互框架反而更清晰——所有 entity type 走同一套 discover → interact → resolve 流程。

## 十三、三大方针校验

- **好维护**：新模块各自独立，职责单一。WorldMapSystem 拆为 4 个子模块（Player/Entity/Exploration/Unlock），每个子模块只有一个变更理由。改角色移动只看 WorldMapPlayer，改实体交互只看 WorldMapEntity，改探索度只看 WorldMapExploration。出 bug 直接定位子模块。启动画面/过场/大地图三者互不影响。实体交互统一走 `interact()` 分发，新增 entity type 只需在 WorldMapEntity 中加一个 case。
- **好管理**：完全遵循现有项目架构模式（`src/systems/` 放逻辑，`src/renderers/` 放渲染，`StateMachine` 管状态，`src/config/` 放数据）。子模块放在 `src/systems/worldmap/` 子目录，与已有的 `fusion/strategies/`、`upgrade/strategies/` 模式一致。`WorldMapConfig` 是纯数据配置，策划可以直接编辑。所有新状态走 StateMachine，渲染走统一 dispatch 表，存档统一走现有 StorageSystem。
- **好扩展**：加新世界只需新增 WorldMapConfig 文件 + 注册到 registry，不改逻辑代码。加新实体类型只需在 WorldMapConfig 中声明 + WorldMapEntity 的 interact 分发加一个 case。碰撞层独立于实体，WorldMapPlayer 的 checkCollision 可从 rect 扩展为 tilemap，不影响上层。过场播放器通用可复用，`entryCutscene` 字段按世界配置。WorldMapSystem 对外接口统一，调用方无需感知内部子模块。