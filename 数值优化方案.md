# 数值优化方案

> 版本：V1 | 日期：2026-05-19 | 作者：数值策划

---

## 一、现状概述

已完成全面数值审计，覆盖战斗伤害公式、养成成长曲线、经济回路、抽卡概率、关卡难度等全链路。以下为关键发现和优化方案。

### 数据来源

- 代码审计：`BattleEngine.js`、`TowerSystem.js`、`CombatSpec.js`、`CharacterConfig.js`、`UpgradeConfig.js`、`GachaSystem.js`、`GameConfig.js`、`MonsterConfig.js` 等 86 个 JS 文件
- 设计文档：`想法02.md`（1093 行完整 GDD）
- 测试基准：`tests/combat-spec.test.mjs`、`tests/simulator/` 场景脚本

---

## 二、伤害公式审计

### 2.1 当前公式

**BattleEngine.js 实测公式**：

```
一星基础伤害 = DAMAGE.BASE + floor × DAMAGE.PER_FLOOR
攻击加成 = 1 + (stats.attack / 100) × 0.5
类型加成 = 1 + sqrt(starTypeMultiplier - 1) × 0.3
快速点击 = 点击间隔 ≤ 200ms → ×4，≤ 250ms → ×2
Combo    = 1 + comboCount × 0.1
暴击     = stats.critDamage（若判定成功）

最终伤害 = floor(基础 × 攻击加成 × 类型加成 × 快速点击 × Combo × 暴击)
```

**攻击力转伤害曲线**（以基础伤害 20 为例）：

| 攻击力 | 攻击加成 | 一星伤害 | 备注 |
|--------|---------|---------|------|
| 10 | ×1.05 | 21 | 新手初始 |
| 50 | ×1.25 | 25 | R 级 Lv.1 |
| 145 | ×1.73 | 34.6 | R 级 Lv.20 |
| 312 | ×2.56 | 51.2 | SSR Lv.30 |
| 800 | ×5.0 | 100 | SSR Lv.60 |
| 1188 | ×6.94 | 138.8 | UR Lv.90 |
| 2345 | ×12.73 | 254.6 | UR Lv.90 ★★★ |

### 2.2 优点

- **平滑压制**：攻击力从 10 到 2345（234 倍差距），伤害只从 21 涨到 255（12 倍差距）。避免了"前期刮痧、后期秒杀"的经典问题
- **Combo 作为节制阀**：20 连断连 → 直接丢失 2/3 伤害，逼迫操作精度始终优先于属性堆叠
- **两个出口**：暴击和快速点击是并行乘区，给不同操作风格的玩家不同的伤害出口

### 2.3 问题

| 问题 | 位置 | 严重度 |
|------|------|--------|
| attackBonus 公式硬编码在 BattleEngine 中 | `BattleEngine.js:159` | 中 |
| quickTapMult 的 ×4/×2 硬编码，不读 CombatSpec | `BattleEngine.js:183` | 中 |
| DAMAGE_VARIANCE（±5% 随机浮动）未实现 | `想法02.md` 有定义 | 低 |
| Tower 模式无统一公式，怪物 HP 靠注释标定 | `TowerSystem.js:58-62` | 高 |

### 2.4 优化方案

**第一步：参数化**。将 `ATTACK_TO_DAMAGE_SCALE`、`ATTACK_DIVISOR`、`DAMAGE_VARIANCE`、`QUICK_TAP_MULT` 纳入 `CombatSpec.DAMAGE` 组，BattleEngine 统一从 `getSpecValue` 读取。

**第二步：Tower 公式打通**。Tower 初始化时通过 `specResolver(COMBAT_SPEC, TOWER_COMBAT_OVERRIDES)` 获得战斗参数，伤害计算使用同一公式。现有怪物 HP 基于此公式重标定。

**第三步：浮动实现**。在 `calculateDamage` 结尾加上 ±5% 随机浮动，增加打击感多样性。

**改动量**：3 个文件，约 40 行修改 + 20 行新增。不改变现有数值行为（参数值保持一致，只是来源变了）。

---

## 三、升星系统审计

### 3.1 当前配置

| 稀有度 | 满级 | 升星材料（同名卡） | 概率链 | 满星倍率 |
|--------|------|-------------------|--------|---------|
| 角色 | 90 级 | [1,1,2,2,3] = 9 张 | 100→80→60→40→25% | ×2.5 |
| 装备 | 15 级 | [1,1,2] = 4 张 | 100→80→60% | ×2.0 |
| 技能 | 10 级 | [1,1,2] = 4 张 | 100→80→60% | ×2.0 |
| 宠物 | 30 级 | [1,1,2] = 4 张 | 100→80→60% | ×2.0 |

### 3.2 问题：UR 性价比倒挂

以满星为目标，对比 UR 和 R：

| 指标 | UR（九鼎之灵） | R（青铜战斧） |
|------|---------------|---------------|
| 满级攻击力 | 2345 | ~240 |
| 升星材料 | 9 张同名 UR | 9 张同名 R |
| 单抽概率 | 0.3% | 20% |
| 期望抽数（同名 9 张）| ~3000 抽 | ~45 抽 |
| 最终攻击力（满星）| 2345×2.5=5862 | 240×2.0=480 |
| 每抽回报 | 5862/3000≈1.95 | 480/45≈10.67 |

**每抽回报，R 是 UR 的 5.5 倍**。如果玩家算明白这笔账，UR 升星是明确的负收益。

### 3.3 优化方案

**方案 A：升星消耗递减 + 失败返还**（推荐）

- 升星失败时返还 50% 的材料（1 张同名卡 → 返还碎片 ×0.5，2 张失败 → 返还 1 张）
- UR 的五星期望消耗从 9 张降至约 6 张，R 从 9 张降至约 7 张
- 同时提升最终成功率级差：25%→35%，让玩家感觉"接近终点时不再完全靠脸"

**方案 B：稀有度差异化消耗**

- R 的 starMaterials 保持不变 [1,1,2,2,3]
- SR 降低为 [1,1,1,2,2]
- SSR 降低为 [1,1,1,1,2]
- UR 降低为 [1,1,1,1,1]
- 高稀有度升星更容易（因为获取更难），低稀有度升星消耗更多（因为获取容易）

**推荐方案 A + B 组合**：升星失败返还机制 + 稀有度差异化消耗。改动量小（仅 `UpgradeConfig.js`），对玩家体验提升大。

---

## 四、Tower 难度曲线审计

### 4.1 当前配置

```javascript
difficultyScale: 1.04  // 每层怪物属性 ×1.04
```

**30 秒内可打伤害估算**（Lv.90 UR，攻击 1188，未升星）：

| 楼层 | 怪物 HP | 难度倍率 | 单星伤害 | 需点数 | 能否 30s 清？ |
|------|---------|---------|---------|--------|-------------|
| 1 | 100,000 | ×1.00 | ~140 | ~715 | 每格约 1 只，20 颗有效星够 |
| 10 | 150,000 | ×1.42 | ~140 | ~1071 | 仍然可清 |
| 20 | 300,000 | ×2.11 | ~140 | ~2143 | 清一个怪需全部 20 颗星 |
| 30 | 700,000 | ×3.24 | ~140 | ~5000 | **不可能，必卡** |
| 50 | 1,500,000 | ×6.83 | ~140 | ~10714 | 完全不可逾越 |

### 4.2 问题

floor 20 是一道软墙（需全部星砸一个怪），floor 30 是硬墙。而 Tower 的 `cellWeights.monster=35`，每层 42% 的格子是怪物。如果 floor 30 有 8 个怪物格，30 秒内完全不可能清完。

### 4.3 优化方案

**分段方案**：前 30 层用温和曲线，30 层后加速。

```javascript
// 当前：全部楼层统一 ×1.04
// 优化：前 30 层 ×1.025，30 层后 ×1.06
```

| 楼层 | 旧 HP | 新 HP | 统一满级能否清？ |
|------|-------|-------|-----------------|
| 10 | 150,000 | 128,000 | 可 |
| 20 | 300,000 | 208,000 | 3-4 颗星清一个，可 |
| 30 | 700,000 | 602,000 | 需升星/装备支撑 |
| 50 | 1,500,000 | 3,020,000 | 必需满星 UR |

前 30 层让未升星的满级玩家通刷，30 层后强制需要升星和装备才能推进——这是"软门槛"的正确放置位置。

**附加方案**：Tower 的 `TOWER_COMBAT_OVERRIDES` 当前为空，可以在此覆盖 `DAMAGE.PER_FLOOR` 让高楼层伤害有额外加成，形成"越爬越爽"的正反馈：

```javascript
TOWER_COMBAT_OVERRIDES = {
    DAMAGE: { PER_FLOOR: 12 },   // 比普通模式的 8 高 50%
    COMBO: { MULTIPLIER: 0.12 }  // 比普通模式的 0.1 高 20%
};
```

---

## 五、Boss 血量三套不一致

### 5.1 现状

| 位置 | 值 | 含义 |
|------|-----|------|
| `GameConfig.js:21` | `bossHp: 150` | 普通模式 Boss 血量 |
| `MonsterConfig.js` | `slime_king.baseHp: 800` | 怪物配置文件中的 Boss |
| `TowerSystem.js:63` | `monsters[0].hp: 100000` | Tower 模式杂兵 HP |

### 5.2 问题

GameConfig 的 150 和 MonsterConfig 的 800 相差 5 倍。需要确认：
- 150 是原始基值，800 是否应用了 `hpGrowth` 或其他系数？
- 如果两套都是活跃值，同一只 Boss 在不同入口的血量会不一致

### 5.3 优化方案

**追溯消费路径，废弃多余定义**：
1. 确认 `GameConfig.bossHp` 的实际消费者（可能只用于旧代码路径）
2. 如果 `MonsterConfig.slime_king.baseHp=800` 是活跃值，将 GameConfig 的 150 标记为 deprecated
3. 统一 Boss 血量源到 `MonsterConfig`

---

## 六、抽卡与经济回路

### 6.1 当前概率

| 稀有度 | 权重 | 单抽概率（近似） | 保底 |
|--------|------|-----------------|------|
| UC | 30 | ~29.5% | - |
| N | 40 | ~39.3% | - |
| R | 20 | ~19.7% | - |
| SR | 8 | ~7.9% | 10 抽保底 |
| SSR | 3 | ~2.9% | 50 抽保底 |
| UR | 0.3 | ~0.29% | - |
| LR | 0.05 | ~0.049% | - |
| SP | 0.01 | ~0.01% | - |

### 6.2 问题

- UR 单抽概率 0.29%，500 抽期望见到第二个 UR（第一个可能保底）。但对只玩碎片时间的玩家，500 抽可能需要 2-3 个月
- 没有 UR 保底机制。如果玩家 200 抽全是 N/R，体验极差
- 保底只有 SR 和 SSR，UR 及以上的高价池完全无保障

### 6.3 优化方案

**追加 UR 软保底**：

- 前 50 抽：标准概率 0.29%
- 51~100 抽：概率每次递增 +0.3%，到 100 抽时约 18%
- 101 抽+：概率每次递增 +0.5%
- 软保底在 120 抽左右触发（期望值）

**理由**：UR 单卡价值已经远超 SSR，玩家期待的是"早晚能得到"的心理安全，而不是"抽到是奇迹"。软保底既保留了惊喜（早期出货），又给了确定性（终极保证）。

---

## 七、伤害公式的 Combo 乘区问题

### 7.1 现状

```
comboMultiplier = 1 + comboCount × 0.1
```

| Combo | 倍率 |
|-------|------|
| 0 | ×1.0 |
| 5 | ×1.5 |
| 10 | ×2.0 |
| 20 | ×3.0 |
| 30 | ×4.0 |

### 7.2 问题

线性成长，无天花板。理论上如果玩家点得够快，Combo 能无限堆。但实际中星星间隔 350ms + 生存 2000ms，同一时间最多 5 颗星在场，自然上限约 15-20 连。

但如果出现灵光密集期（D1 开局阶段），6-8 颗星同时在场，Combo 可能短暂冲到 25+，此时伤害是基线的 3.5 倍。配合暴击和快速点击，会出现"3 秒内秒掉 Boss"的情况。

### 7.3 优化方案

不改 Combo 公式本身（当前体验大部分时候是好的），而是增加**连击衰减**：

```
comboMultiplier = 1 + comboCount × 0.1 (前 15 连)
comboMultiplier = 2.5 + (comboCount - 15) × 0.05 (15 连以后)
```

| Combo | 旧倍率 | 新倍率 |
|-------|--------|--------|
| 15 | ×2.5 | ×2.5（不变） |
| 20 | ×3.0 | ×2.75 |
| 30 | ×4.0 | ×3.25 |

15 连后收益减半。维持高 Combo 仍然有价值，但不会出现"秒 Boss"的极端情况。

---

## 八、实施优先级

| 优先级 | 项目 | 改动量 | 风险 | 玩家可感知 |
|--------|------|--------|------|-----------|
| **P0** | 伤害公式参数化 + Tower 打通 | 3 文件 / 60 行 | 低（不改数值） | 否（行为一致） |
| **P0** | Boss 血量统一 | 2 文件 / 10 行 | 低 | 否 |
| **P1** | Tower 难度分阶 + 伤害加成 | 1 文件 / 15 行 | 中（需测试） | 是（爬到 30 层不再卡） |
| **P1** | 升星失败返还 + 消耗差异化 | 1 文件 / 20 行 | 中 | 是（升星体验从"赌"变"刷"） |
| **P2** | UR 软保底 | 1 文件 / 25 行 | 中 | 是（长期留存关键） |
| **P2** | Combo 衰减 | 1 文件 / 5 行 | 低 | 细微（仅 Combo 20+ 时） |
| **P3** | 伤害浮动 ±5% 实现 | 1 文件 / 3 行 | 极低 | 细微 |

---

## 九、验证方法

每项优化后，通过以下方式验证：

1. **单元测试**：`tests/combat-spec.test.mjs` 确保 CombatSpec 的 specResolver 和 getSpecValue 不退化
2. **模拟器**：`tests/simulator/run.js` 模拟 1000 局自动战斗，采集伤害分布和通关率
3. **DevBattle**：`DevBattleSystem.js` 手动验证手感变化
4. **数据对比**：每项改动前后，录制关键数值点（Lv.1/30/90 单星伤害、Tower floor 20 通关率、升星期望消耗）

---

## 十、补充审计：新增问题（第二轮）

以下问题在首轮审计中被遗漏，经第二轮全子系统审计后确认。

---

### 10.1 融合系统：稀有度只 +1（致命）

**现状**：`FusionConfig.js` 定义了初融/超融/极融三层，`layerMultipliers` 为 ×2/×5/×15，但 `getNextRarity()` 永远只将稀有度 +1 级（沿 `RARITY_ORDER` 索引 +1）。

**文档定义**：初融稀有度 +1、超融 +2、极融 +3。

**结论**：极融名存实亡。玩家投入 3 张高稀有度材料、面对 20% 成功率，产出只比初融多跳了一级稀有度。×15 的 stats 倍率是唯一价值，但稀有度（直接影响攻击力基值）没有提升。

**优化方案**：

```javascript
// 修改 getNextRarity 接受 layer 参数
getNextRarity(currentRarity, layer) {
    var steps = layer === 'normal' ? 1 : layer === 'super' ? 2 : 3;
    var idx = RARITY_ORDER.indexOf(currentRarity);
    return RARITY_ORDER[Math.min(idx + steps, RARITY_ORDER.length - 1)];
}
```

---

### 10.2 灵宠系统：实现完整偏离文档（致命）

**文档定义**（`想法02.md`）：出战 1 只，提供属性加成 + 自动拾取战斗掉落物；不主动攻击，不占用操作；8 种被动队伍加成；3 阶进化链；拾取范围随等级提升。

**代码实现**：PetSystem 实现的是"自动攻击怪物"，不是被动加成；无自动拾取、无被动队伍加成、无进化链、无拾取范围。

**优化方案**（需开发配合）：移除灵宠主动攻击逻辑，实现 8 种被动加成按文档配置；自动拾取和进化链延期到 V2。

---

### 10.3 技能伤害：与角色攻击力无关联（致命）

**现状**：技能伤害是写死的绝对值。满级 UR（1188 攻）下，SSR 技能 400 伤害 ≈ 1.4 颗普通星的伤害——贡献不足 10%。

**优化方案**：技能伤害改为百分比公式。

```javascript
skillDamage = stats.attack × skillAttackRatio × (1 + comboCount × 0.05)
```

| 稀有度 | 旧固定值 | 新 skillAttackRatio | 满级 UR 下伤害 |
|--------|---------|---------------------|---------------|
| R | 120 | 1.0 | ~1188 |
| SR | 280 | 2.5 | ~2970 |
| SSR | 500 | 4.0 | ~4752 |
| UR | 1200 | 10.0 | ~11880 |
| LR | 1500 | 15.0 | ~17820 |

---

### 10.4 材料：4 种无掉落源 + 可无限叠加（严重）

以下材料定义了属性加成但无掉落源：`devourerResidue`、`dragonScale`、`darkEssence`、`starHeart`。需补充掉落源或标记 deprecated。

水灵晶/火灵源每次使用 +1/+2 攻击，无次数上限，需加每角色使用上限（建议 50 次）。

---

### 10.5 挂机金币与消耗严重不匹配（严重）

挂机基础收益 2 灵币/分钟，满加成日收益 2400 灵币。装备融合 SSR 消耗 5000 灵币/次——挂机一天不够融合一次。

**优化方案**：`baseGoldPerMinute: 2 → 10`，满加成日收益 → 12000 灵币。同时修复 preview 与 claim 材料计算不一致（preview 用 `effectiveMinutes/60`，claim 用 `effectiveMinutes/120`）。

---

### 10.6 怪物攻击 vs 玩家 HP 失衡（关注）

UR 级怪物攻击 100/1s，满级角色 HP 约 3000，无治疗时 30 秒存活堪忧。建议怪物攻击增长从指数（×1.10/级）降为次线性（×1.05/级）。

---

## 十一、汇总优先级（含 Tower 深度审计）

| 优先级 | 项目 | 类型 | 改动量 | 玩家可感知 |
|--------|------|------|--------|-----------|
| **P0** | 觉醒 HP 缩放修正（0.15→0.05） | 数值 | 1 文件 / 1 行 | 是（Boss 不再不可杀） |
| **P0** | 伤害公式参数化 + Tower 打通 | 数值 | 3 文件 / 60 行 | 否 |
| **P0** | Boss 血量统一 | 数值 | 2 文件 / 10 行 | 否 |
| **P0** | 融合稀有度修复（+1→+2/+3） | 逻辑 | 1 文件 / 8 行 | 是 |
| **P0** | Tower 精英/Boss HP 倍率修正（×3→×1.5, ×8→×3） | 数值 | 1 文件 / 4 行 | 是 |
| **P1** | Tower 难度分阶 + 怪物格概率 35%→20% | 数值 | 1 文件 / 10 行 | 是 |
| **P1** | Tower 战斗时间调整（前 20 层 40s） + 奖励 ×3 | 数值 | 1 文件 / 8 行 | 是 |
| **P1** | TOWER_COMBAT_OVERRIDES 启用 | 数值 | 1 文件 / 6 行 | 细微 |
| **P1** | HEAL_HP/SHIELD 改为百分比（5%/3% maxHp） | 数值 | 1 文件 / 3 行 | 是 |
| **P1** | 经验公式 100×level→100+level×50 | 数值 | 1 文件 / 3 行 | 是（升级不再痛苦） |
| **P1** | 升星失败返还 + 消耗差异化 | 数值 | 1 文件 / 20 行 | 是 |
| **P1** | 技能伤害百分比化 | 数值+逻辑 | 2 文件 / 40 行 | 是 |
| **P1** | 挂机金币提升 + bug 修复 | 数值 | 1 文件 / 8 行 | 是 |
| **P2** | 灵宠被动加成实现 | 数值+逻辑 | 2 文件 / 50 行 | 是 |
| **P2** | UR 软保底 | 数值 | 1 文件 / 25 行 | 是 |
| **P2** | 材料掉落源补全 + 使用上限 | 数值 | 2 文件 / 15 行 | 细微 |
| **P2** | Combo 衰减 | 数值 | 1 文件 / 5 行 | 细微 |
| **P2** | Tower 毒伤可被护盾吸收 | 逻辑 | 1 文件 / 5 行 | 是 |
| **P2** | Boss 眩晕概率上限 25% | 数值 | 1 文件 / 3 行 | 是 |
| **P2** | 在 MonsterConfig 中定义 UC 怪物 | 数值 | 1 文件 / 15 行 | 是 |
| **P3** | 觉醒分数缩放 0.05→0.02 | 数值 | 1 文件 / 1 行 | 细微 |
| **P3** | 伤害浮动 ±5% | 数值 | 1 文件 / 3 行 | 细微 |
| **延期** | 灵宠进化链/自动拾取 | 架构 | - | V2 |

---

## 十二、灵塔试炼（Tower）深度数值审计

以下对 Tower 模式的全部数值维度进行逐项分析，基于 `TowerSystem.js`（2164 行）的实际代码逻辑。

---

### 12.1 战斗架构

Tower 不独立计算伤害，而是委托给 `BattleEngine`：

```
玩家移动 → 踩到怪物格 → startCombat()
  → 构建 engineConfig（monster / playerStats / pet / skills）
  → battleEngine.init(engineConfig)
  → BattleEngine 接管星星生成 / 伤害计算 / 状态管理
  → 每 1 秒同步状态回 Tower（combatTime / playerHp / monsterHp）
```

**核心含义**：Tower 的星星伤害公式与普通模式完全相同。两者差异仅在于怪物 HP 和战斗时间。`TOWER_COMBAT_OVERRIDES` 当前为空（`{}`），Tower 没有获得任何专属战斗参数。

---

### 12.2 怪物 HP 公式

```javascript
// TowerSystem.js:500-535
difficultyMult = 1.04 ^ (floor - 1)
floorAttackBonus = 1 + (floor - 1) × 0.02  // 仅用于攻击力，不用于 HP
hpMult (sizeType): normal = 1, elite = 3, boss = 8

最终 HP = 基础HP × difficultyMult × hpMult
```

---

### 12.3 HP-DPS 匹配度分析

以满级 UR 角色（攻击=1188，未升星，无装备）为基准。BattleEngine 公式下：

- 一星基础伤害 = 20 × (1 + 1188/100 × 0.5) = **138.8**
- 30 秒战斗，350ms 星星间隔 → 最多 85 颗星
- 扣除特殊星（回复/护盾/闪避约 40%）→ 有效攻击星约 **50 颗**
- 带 Combo 10 连（×2.0）+ 暴击 30%期望（×1.5）：**50 × 139 × 2.0 × 1.5 ≈ 20850**

理论最大输出约 **20,850 伤害/30 秒**（不含装备/升星额外加成）。

| 楼层 | difficultyMult | 基础怪 HP | 实际 HP | 精英 HP(×3) | Boss HP(×8) | 需星数(普通) | 30s 可杀？ |
|------|---------------|-----------|---------|------------|------------|------------|----------|
| 1 | 1.00 | 100,000 | 100,000 | 300,000 | - | 24 | 可 |
| 5 | 1.17 | 200,000 | 234,000 | 702,000 | 3,200,000 | 56 | 普通可，**精英不可** |
| 10 | 1.42 | 300,000 | 426,000 | 1,278,000 | - | 102 | 渐紧 |
| 15 | 1.73 | 400,000 | 692,000 | 2,076,000 | 7,200,000 | 166 | 普通不可 |
| 20 | 2.11 | 500,000 | 1,055,000 | 3,165,000 | 12,000,000 | 253 | **全面不可** |
| 30 | 3.12 | 700,000 | 2,184,000 | 6,552,000 | 28,000,000 | 524 | 绝对不可 |

**结论**：floor 5 的精英怪（HP=70.2 万）已超出未升星满级角色的 30 秒输出上限。floor 15+ 普通怪也无法单次击杀。整个难度曲线从 floor 5 开始断裂。

---

### 12.4 精英/Boss 的体积与 HP 倍率

```javascript
elite（floor 5+，15%概率，floor 每+1 概率+1%）：HP ×3，ATK ×1.5，占 2×2 = 4 格
boss（floor 20+，5%概率）：HP ×8，ATK ×2.5，占 4×2 = 8 格
```

**问题**：HP 倍率 3 和 8 是灾难性的。一个 floor 5 精英 slime 的 HP = 100k × 1.04^4 × 3 = 350,400。即使 50 颗星全部暴击（期望不可达），也只能打约 50 × 139 × 2 = 13,900。精英 HP 是理论输出的 25 倍。

---

### 12.5 网格权重与探索时长

```javascript
cellWeights: { empty:42, monster:35, treasure:5, material:10, trap:8 }
// monster 格概率 = 35/100 = 35%
// 20×20 网格 = 400 格，约 140 个怪物格
```

玩家探索 50 格/层，约遇到 **17.5 场战斗**。每场 30 秒 = **525 秒/层 ≈ 8.75 分钟**。

配合 floor 15+ 每只怪需要 2-3 次尝试（因为无法单次击杀），实际每层耗时可能 **15-20 分钟**。对"碎片时间"的小游戏定位来说，太长了。

---

### 12.6 怪物攻击 vs 玩家生存

| 楼层 | 怪物 ATK | 攻击间隔 | DPS | 中毒附加 | 总 DPS | 玩家 HP | 存活时间 |
|------|---------|---------|-----|---------|--------|---------|---------|
| 1 | 40 | 2000ms | 20 | 0 | 20 | ~500(Lv.10) | 25s |
| 10 | 70 | 1800ms | 39 | 0 | 39 | ~1000(Lv.30) | 25s |
| 20 | 90 | 1500ms | 60 | 0 | 60 | ~1500(Lv.50) | 25s |
| 30 | 100 | 1500ms | 67 | 3×3s | 76 | ~2000(Lv.70) | 26s |
| 50 | 130 | 1500ms | 87 | 8×5s | 95 | ~3000(Lv.90) | 31s |

存活时间约 25-31 秒，与战斗时间 30 秒几乎完全匹配——这不是平衡，是**恰好死在超时边缘**。中毒伤害（3→8/秒）和眩晕（15~20% 几率）进一步压缩实际操作时间。

---

### 12.7 中毒机制

```javascript
// TowerSystem.js:1058-1069
// 怪物攻击命中后，有概率附加中毒（无视护盾）
// 中毒每次 tick 先被护盾吸收，剩余直接扣 HP
// 毒伤范围：3（floor 5 Boss）~ 8（floor 50 Boss）
```

**问题**：中毒无视操作——玩家无法通过闪避/护盾星星完全免疫，只能硬吃。在 floor 15+ 的中毒 Boss 战中，毒伤占总伤害的 30-40%。护盾星星在 Tower 中的价值因此打折。

---

### 12.8 奖励链 vs 时间投入

```javascript
baseRewards: { gold: [10,50], starSource: [1,5], materialChance: 0.3 }
bossFloorRewards: { gold: [100,300], starSource: [10,30], materialChance: 0.5, equipmentChance: 0.05 }
```

对比普通模式一局 30 秒：击杀 5-8 只怪 + Boss，掉落远超 Tower 单场。Tower 的**单位时间回报**远低于普通模式——玩家没有理由选择爬塔。

---

### 12.9 超时保存 / 再次挑战

```javascript
// combatTimeout: 保存 monster.savedHp
// 再次点击同一格子 → 从上次剩余 HP 开始继续战斗
```

设计意图是好的（允许"磨"死怪物），但 floor 15+ 每只怪需要 2-5 次才能击杀，配合 17.5 场/层，每层可能需要 **40-80 次战斗**。探索感被反复的"进战斗→打不过→超时→再进战斗"循环破坏。

---

### 12.10 技能在 Tower 中的特殊路径

```javascript
// TowerSystem.js:1123-1185
// 技能伤害读 SkillConfig 的绝对值（不参与 BattleEngine 公式）
// 只有 buff 类型技能用 stats.attack × 0.5 作为伤害
```

这意味着技能百分比化改造（10.3 节）也会同步改善 Tower——`buff` 类型是唯一的例外路径。

---

### 12.11 优化方案

**方案总表**：

| 改动 | 位置 | 效果 |
|------|------|------|
| 精英 HP×3→×1.5，Boss HP×8→×3 | `generateMonster()` | floor 5 精英可杀 |
| difficultyScale 分阶：1-20 层 ×1.02，21-40 层 ×1.04，41+ ×1.06 | `TOWER_CONFIG` | 前 20 层探索友好 |
| 怪物格概率 35%→20% | `cellWeights.monster` | 每层从 17 场降到 10 场 |
| 战斗时间 30s→40s（前 20 层）/ 25s（21 层+） | `startCombat()` | 前中期容错 |
| TOWER_COMBAT_OVERRIDES 启用 | `CombatSpec.js` | Tower 获得专属 DAMAGE.PER_FLOOR 加成 |
| 基础奖励 ×3 | `TOWER_CONFIG.baseRewards` | 单位时间回报追上普通模式 |
| 毒伤可被护盾完全吸收 | `monsterAttackPlayer()` | 护盾星星恢复价值 |

**精英/Boss HP 修正后的效果**：

| 楼层 | 旧精英 HP | 新精英 HP(×1.5) | 旧 Boss HP | 新 Boss HP(×3) | 满级未升星可杀？ |
|------|----------|----------------|-----------|---------------|----------------|
| 5 | 702,000 | 351,000 | 3,200,000 | 1,200,000 | 精英勉强/需升星，Boss 需 3 次 |
| 15 | 2,076,000 | 1,038,000 | 7,200,000 | 2,700,000 | 精英可 1 次，Boss 需 2-3 次 |
| 20 | 3,165,000 | 1,582,500 | 12,000,000 | 4,500,000 | 精英可 1 次（需 Combo），Boss 需 3-4 次 |
| 30 | 6,552,000 | 3,276,000 | 28,000,000 | 10,500,000 | 必须升星 + 装备 |

---

### 12.12 Tower 问题汇总

| # | 问题 | 严重度 | 当前后果 |
|---|------|--------|---------|
| 1 | 怪物 HP 远超 30 秒 DPS 上限 | **致命** | floor 5 精英不可杀 |
| 2 | 精英 HP×3 / Boss HP×8 倍率过高 | **致命** | 整个难度曲线从 floor 5 崩坏 |
| 3 | 怪物格 35% → 每层 17 场战斗 | 高 | 探索时间过长（8-15 分钟/层） |
| 4 | 单位时间回报低于普通模式 | 高 | 玩家无动力爬塔 |
| 5 | `TOWER_COMBAT_OVERRIDES = {}` | 中 | 无 Tower 专属战斗参数 |
| 6 | 中毒无视护盾 | 中 | 护盾星星在 Tower 中价值降低 |
| 7 | 超时保存导致循环"磨血" | 中 | 破坏了探索节奏 |
| 8 | 怪物攻击 DPS 精确匹配 30 秒存活 | 中 | 无容错空间 |

---

## 十三、普通模式（BattleEngine）深度数值审计

以下对灵域探索 / 守护灵战的全部数值维度进行逐项分析，基于 `BattleEngine.js`（1175 行）、`MonsterSpawnSystem.js`（540 行）、`NormalBattleAdapter.js`（1895 行）、`MonsterConfig.js`（980 行）、`GameConfig.js`（195 行）的实际代码逻辑。

---

### 13.1 战斗框架

普通模式通过 `NormalBattleAdapter` 委托给 `BattleEngine`：

```
玩家点击星星 → NormalBattleAdapter 拦截
  → battleEngine.handleStarClick(x, y, externalStars)
  → BattleEngine.calculateDamage() 计算伤害
  → BattleEngine.fireAttackerProjectile() 处理怪物攻击
  → 回调 onMonsterDeath / onPlayerDeath / onTimeUp 由 Adapter 处理
```

**核心含义**：普通模式和 Tower 共享同一套 BattleEngine，但使用 `COMBAT_SPEC` 基线配置（无 `TOWER_COMBAT_OVERRIDES`）。这意味着对 BattleEngine 的公式改造会同时影响两种模式。

---

### 13.2 怪物生成与 HP 公式

```javascript
// MonsterSpawnSystem.js:340-400
// 普通怪物（非 Boss）HP:
levelMultiplier = 1.15 ^ killCount
maxHp = Math.floor(baseHp × levelMultiplier)
// score >= 2000 时增强：×1.5

// Boss HP:
maxHp = baseHp  // 不使用 killCount 成长
// score >= 2000 时增强：×2

// 觉醒系统缩放（玩家 Lv > 25 时）：
awakeHpScale = (1.15) ^ (playerLevel - 25)
awakeAtkScale = (1.04) ^ (playerLevel - 25)
maxHp = maxHp × awakeHpScale
monsterAttack = monsterAttack × awakeAtkScale
```

**一局 30 秒内的怪物递增**：

| 击杀数 | 类型 | 基数 HP | levelMult(1.15^kills) | 实际 HP(未增强) | 玩家攻击~300 时需星数 |
|--------|------|---------|---------------------|----------------|---------------------|
| 1 | slime | 100 | 1.15 | 115 | ~2 颗 |
| 3 | slime | 100 | 1.52 | 152 | ~3 颗 |
| 5 | slime | 100 | 2.01 | 201 | ~4 颗 |
| 8 | Boss | 800 | 不成长 | 800 | ~16 颗 |

30 秒战斗中玩家通常击杀 5-8 只普通怪 + 1 只 Boss。单星伤害约 100-150（Lv.30 角色攻击力约 300），50 颗有效星 × 125 = 6250。击杀 5 只普通怪消耗约 15 颗星，Boss 消耗约 16 颗星，总计约 31 颗——**刚好匹配**。

---

### 13.3 稀有度权重 vs 分数解锁

```javascript
// MonsterConfig.js
MonsterRarityWeights: { UC:60, N:50, R:30, SR:15, SSR:4, UR:1 }
```

| 稀有度 | 权重 | 概率 | 代表怪物 | 分数解锁 | 单局出现概率 |
|--------|------|------|---------|---------|------------|
| UC | 60 | 37.5% | - | - | 几乎不出现（权重高但无 UC 怪物定义） |
| N | 50 | 31.3% | 铜锈碎片(HP100)、碎瓷灵(HP80) | 60-100 | 最常见 |
| R | 30 | 18.8% | 锈剑残魂(HP90)、墨渍灵(HP110) | 200-400 | 常见 |
| SR | 15 | 9.4% | 碎瓷聚合体(HP450)、狂草灵(HP450) | 600-800 | 偶尔 |
| SSR | 4 | 2.5% | 铜龙幼灵(HP600)、丹青妖卷(HP350) | 1000-1400 | 稀有 |
| UR | 1 | 0.6% | 龙纹鼎灵(HP800)、虚空裂片(HP700) | 1600-1800 | 极稀有 |

**问题**：UC 权重 60（最高）但 MonsterConfig 中没有 UC 稀有度的怪物。`getRandomMonster` 选择 UC 时会 fallback 到 N 级——所以实际战斗中约 68.8% 概率出现 N 级怪。这导致战斗的怪物多样性在前 600 分（解锁 SR 之前）非常有限。

---

### 13.4 觉醒系统（Awake）的影响

```javascript
// GameConfig.js:90-105
// Lv.25 起触发
hpGrowthRate: 0.15    // 每级怪物 HP ×1.15
atkGrowthRate: 0.04   // 每级怪物攻击 ×1.04
scoreGrowthRate: 0.05  // 每级分数 ×1.05（远低于难度，防止分数膨胀）

// 觉醒阶段：
Lv.30: armor(减伤15%)
Lv.40: rage(血量50%以下攻击×1.5)
Lv.50: dodge(15%闪避)
Lv.60: resist(50%抗性)
```

**一局 30 秒中觉醒系统的影响**（以 Lv.50 玩家为例）：

| 击杀数 | 基础 HP | 觉醒 HP 缩放 | 实际 HP(未增强) | 无觉醒时 | 有觉醒时需星 |
|--------|---------|-------------|---------------|---------|------------|
| 1(slime) | 100 | (1.15)^25=32.9x | 3,290 | 2 | ~7 |
| 3(slime) | 100 | 32.9×1.52=49.9x | 4,990 | 3 | ~10 |
| 8(Boss) | 800 | 32.9x(不成长) | 26,320×2(Boss增强) | 16 | ~105 |

**致命发现**：Lv.50 玩家面对的 Boss HP = 800 × (1.15)^25 × 2(Boss增强) ≈ 52,640。此时单星伤害约 150-200。50 颗有效星只能打 7500-10000。Boss 需要 **263-350 颗有效星**——远远超出 30 秒的物理极限。

这意味着 **Lv.30+ 的玩家在普通模式中无法击杀 Boss**。觉醒系统使怪物 HP 指数膨胀的速度远快于玩家攻击力线性增长。这不是设计意图——是数值崩溃。

**觉醒 HP 缩放修正**：

| 玩家等级 | 旧觉醒 HP | 修正 HP(1.05^差值) | Boss HP(旧) | Boss HP(新) |
|---------|----------|-------------------|-------------|-------------|
| 30 | ×2.01 | ×1.28 | 1,600 | 1,024 |
| 40 | ×8.14 | ×2.08 | 6,512 | 1,664 |
| 50 | ×32.9 | ×3.39 | 52,640 | 5,424 |
| 60 | ×133.2 | ×5.52 | 213,120 | 17,664 |

修正方案：`hpGrowthRate: 0.15 → 0.05`，`scoreGrowthRate: 0.05 → 0.03`（同步降低分数膨胀）。

---

### 13.5 普通模式特殊星星数值

```javascript
// CombatSpec.SPECIAL_STARS
HEAL_HP: 15        // 回复 15 HP（玩家 HP 100-3000，回复率 0.5%-15%）
SHIELD_AMOUNT: 10  // 护盾 10（吸收约 1 次攻击或半次）
TIME_SECONDS: 3    // +3 秒（10% 战斗时间）
UNLUCKY_HP_COST: 5 // 厄运 -5 HP，3 次触发怒气效果
GREEDY_HP_COST: 1  // 贪婪 -1 HP，累积贪池
```

**HEAL_HP=15 的问题**：Lv.1 玩家 HP=100，回复 15%=15 点——合理。但 Lv.90 玩家 HP 约 3000，15 点仅 0.5%。治疗星星在后期几乎无价值。

建议：`HEAL_HP` 改为百分比（如 5% 最大 HP），或按玩家等级线性缩放。

**SHIELD_AMOUNT=10**：怪物攻击约 10-100，护盾只能吸收约 1 次到 0.1 次攻击。护盾星星的价值同样在后期衰减。

---

### 13.6 怒气系统（Unlucky Star）

```javascript
// 每次点击 unlucky 星：-5HP + rage+1
// rage=3 时：33% 概率暴击(2×attack)，33% 概率 +50 攻击，33% 概率 +30HP
```

**价值分析**：3 次 unlucky 消耗 15HP，期望回报约 (2×atk + 50atk + 30HP)/3。以 Lv.30 玩家攻击=300 为例，期望回报 = (600+50+30)/3 = 227。15HP 换 227 输出——但 1/3 概率只获得 50 攻击加成（而非伤害），且可能触发空包（回复 30HP，净亏 15HP）。**期望值为正但方差极大**。

---

### 13.7 怪物攻击 vs 玩家生存

```javascript
// 普通模式 30 秒，受击 -5 秒
// slime(ATK=10, int=2000ms) → DPS=5
// 龙纹鼎灵(ATK=80, int=1200ms) → DPS=67
```

| 怪物 | ATK | 间隔 | DPS | 30s 总伤 | 玩家 HP(Lv.30,~1000) | 存活 |
|------|-----|------|-----|---------|----------------------|------|
| 铜锈碎片 | 10 | 2000ms | 5 | 150 | 1000 | 200s(远超) |
| 狂草灵 | 45 | 1600ms | 28 | 840 | 1000 | ~36s(刚好) |
| 龙纹鼎灵 | 80 | 1200ms | 67 | 2010 | 1000 | ~15s |

普通怪物的伤害在合理范围内——玩家 30 秒内不会被普通怪打死。但 Boss（UR 级龙纹鼎灵 DPS=67 + 30% 眩晕 + 50% 狂暴）会在约 10 秒内击杀 Lv.30 玩家。**Boss 的机制（眩晕 + 狂暴）和数值（67 DPS）叠加导致的致死时间过短**。

---

### 13.8 Boss 机制数值

```javascript
// Boss 专属机制：
slime_king: split(50%HP→3只slime) + poison(20伤害/5秒/8毒伤)
goblin_king: summon(每12秒2只goblin)
flame_lord: stun(20%1.5秒) + rage(30%HP→ATK×2)
ice_queen: stun(25%2秒) + shield(每8秒+100)
thunder_dragon: stun(35%1.5秒) + doubleAtk(30%)
void_emperor: stun(35%2秒) + absorb(吸收所有星星回血) + silence(禁用技能)
```

**分裂机制的数值问题**：slime_king(HP=800) 在 50% HP(400) 时分裂成 3 只 slime(HP=100)。分裂后总 HP 从 400 变成 3×100=300——实际上减少了。但这是故意设计：先处理分裂体再杀 Boss 本体。

**吸收星星回血的机制**：ice_queen 吸收 50% 冰星回血(×0.8)，flame_lord 吸收 50% 火灵星回血(×0.8)。以火焰领主 HP=2000 为例，一颗火灵星伤害约 150，50%×0.8=60 回复。每次攻击火灵星相当于给 Boss 回复 40% 的伤害量——**导致星星类型选择的策略深度**，但也有可能让战斗无法终结（如果玩家不知道要避开火灵星）。

---

### 13.9 drop 奖励链

```javascript
// MonsterConfig.drops:
slime:      { gold: [5,15], exp:5 }
goblin:     { gold: [5,20], exp:8 }
skeleton:   { gold: [10,30], exp:12 }
demon:      { gold: [50,100], exp:55 }

// Boss drops:
slime_king:      { gold:100, exp:80, materials:['iceCrystal'], materialChance:1.0 }
flame_lord:      { gold:250, exp:200, materials:['fireSource','critFireSource'], materialChance:0.8 }
void_emperor:    { gold:500, exp:400, materials:['critCrystal','critFireSource'], materialChance:0.6 }
```

一局 30 秒的典型掉落：
- 5 只普通怪（N~R 级）：灵币 25-100，经验 25-60
- 1 只 Boss（SR 级）：灵币 100，经验 80，材料 ×1（100%）

总计约 125-200 灵币 + 105-140 经验 + 1 材料。升到 90 级需要 100×1 + 100×2 + ... + 100×90 = 409,500 经验。约需 **3000-4000 局**才能升满一个角色。配合"100×level"的经验公式，后期每级需要 9000 经验 = 约 90 局。

---

### 13.10 普通模式问题汇总

| # | 问题 | 严重度 | 当前后果 |
|---|------|--------|---------|
| 1 | **觉醒 HP 缩放导致 Boss 无法击杀** | **致命** | Lv.30+ 玩家 Boss 战不可能通过 |
| 2 | HEAL_HP=15 / SHIELD=10 不随等级缩放 | 高 | 后期治疗/护盾星星无价值 |
| 3 | UC 权重 60 但无 UC 怪物定义 | 中 | 前 600 分怪物多样性低 |
| 4 | Boss 眩晕 + 狂暴 + 高 DPS 致死过快 | 中 | Lv.30 玩家 10 秒内被杀 |
| 5 | 怒气系统方差极大 | 中 | 33% 概率空包（净亏 HP） |
| 6 | 升到满级需 3000-4000 局 | 中 | 经验曲线过陡 |
| 7 | 吸收星星回血导致 Boss 不可终结 | 低 | 需要玩家知道避开特定星星 |

### 13.11 普通模式优化方案

| 改动 | 位置 | 效果 |
|------|------|------|
| 觉醒 HP 缩放 0.15→0.05 | `GameConfig.js` | Lv.50 Boss 从 52640→5424 |
| HEAL_HP 改为 5% maxHp | `CombatSpec.SPECIAL_STARS` | 治疗星星全期有效 |
| SHIELD 改为 3% maxHp | `CombatSpec.SPECIAL_STARS` | 护盾星星全期有效 |
| 觉醒分数缩放 0.05→0.02 | `GameConfig.js` | 防止分数膨胀 |
| Boss 眩晕概率上限 25% | `MonsterConfig.void_emperor` | 致死时间延长 |
| 经验公式 100×level→100+level×50 | `UpgradeConfig.js` | 满级约 1200 局（降低 3 倍） |
| 在 MonsterConfig 中定义 UC 怪物 | `MonsterConfig.js` | 增加低分怪物多样性 |