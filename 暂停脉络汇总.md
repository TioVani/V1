# 战斗舞台暂停脉络汇总

> 分析范围：`e:\AIGame\V1\`，涵盖所有战斗模式（普通、Boss、塔、闯关、赛季）。

---

## 一、暂停触发入口

所有暂停最终由用户点击 UI 暂停按钮或广告事件触发，按模式分流：

```
用户点击 ⏸ 按钮 (game.js:3918)
  │
  ├─ 1. pauseAnimations()         ← 冻结动画虚拟时钟
  │
  └─ 2. 按模式分发：
       ├─ 闯关模式  → StageModeSystem.togglePause()
       ├─ Boss模式  → BossBattleAdapter.togglePause()
       ├─ 塔模式    → ModeLifecycleManager.pauseActive()
       └─ 普通模式  → ModeLifecycleManager.pauseActive() + GameLifecycleSystem.pauseGame()
```

| 触发源 | 入口文件 | 行号 | 说明 |
|--------|---------|------|------|
| 暂停按钮点击 | `game.js` | 3918-3933 | 检测左上角 50×50 区域，先冻结动画，再按模式分发 |
| 广告展示 | `AdSystem.js` | 200 | 调 `pauseGameTimers()` |
| 塔退出 | `TowerRenderer.js` | 901, 932 | 退出/停止时调 `towerSystem.pauseTower()`，持久化进度 |

---

## 二、各系统暂停状态变量

| 系统 | 文件 | 变量 | 含义 |
|------|------|------|------|
| 全局状态机 | `StateMachine.js` | `state === PAUSED` | 全局暂停标志 |
| 动画系统 | `AnimationSystem.js` | `_pauseAccumulated` / `_pauseStartTime` | 虚拟冻结时钟，替代 `Date.now()` |
| 战斗引擎 | `BattleEngine.js` | `phase === 'paused'` + `pauseStartTime` | 战斗内部暂停标记 |
| Boss 战斗 | `BossBattleAdapter.js` | `_paused` | Boss 模式暂停状态 |
| 闯关模式 | `StageModeSystem.js` | `isPaused` (行 65) | 闯关暂停标志 |
| 塔模式 | `TowerSystem.js` | `playerData.infiniteTower.isPaused` | 塔进度持久化暂停 |
| 偷星者 | `StarThiefSystem.js` | `paused` / `pausedAt` (行 27-28) | 偷星者独立暂停 |
| 战斗渲染 | `GameBattleRenderer.js` | `_paused` (行 199) | 聚合 `state===PAUSED` 和 `_bossPaused` |

---

## 三、暂停时间补偿骨架

全局暂停时长计算后统一补偿给所有基于时间推进的系统：

```
pauseDuration = Date.now() - pauseStartTime

补偿对象（在 resume() 中逐一加 pauseDuration）：
  ├─ BattleEngine (行 1125-1146)
  │   ├─ comboStarStartTime      ← 连击灵光计时器
  │   ├─ poisonEndTime           ← 中毒结束时间
  │   ├─ poisonTickTime          ← 中毒跳伤时刻
  │   ├─ stunEndTime             ← 眩晕结束时间
  │   ├─ dodgeEndTime            ← 闪避结束时间
  │   ├─ 所有攻击者 lastAttackTime  ← 怪物/玩家攻击间隔
  │   ├─ pendingDeaths[].deathTime  ← 死亡时序
  │   └─ petLastAttackTime       ← 宠物攻击间隔
  │
  ├─ BossBattleAdapter (行 874-910)
  │   ├─ 所有星星 disappearTime  ← 星消失时间
  │   └─ 连击时间重置
  │
  ├─ GameLifecycleSystem (行 481-536)
  │   ├─ 所有星星 disappearTime
  │   └─ 连击时间重置
  │
  └─ StarThiefSystem (行 241-249)
      └─ breakEndTime            ← 偷星者破防结束时间
```

---

## 四、暂停时清除的定时器 / 阻塞的逻辑

暂停后，以下所有推进全部停止：

| 清除项 | 负责系统 | 方法 |
|--------|---------|------|
| 动画时钟冻结 | `AnimationSystem` | `getGameTime()` 返回冻结时间 |
| 战斗相位冻结 | `BattleEngine` | `phase = 'paused'` |
| 主计时器 | `GameLifecycleSystem` | `clearTimerInterval()` |
| 星生成间隔 | `GameLifecycleSystem` | `clearMoveInterval()` |
| 怪物攻击间隔 | `GameLifecycleSystem` | `clearMonsterAttackInterval()` |
| 偷星者发射 | `StarThiefSystem` | `clearTimeout(starTimer)` |
| 闪避星 | `StageModeSystem` | `stopDodgeStarTimer()` |
| 宠物攻击 | `BossBattleAdapter` | `clearInterval(petAttackTimer)` |
| 模式定时器 | `ModeLifecycleManager` | 清理当前模式所有 `TimerManager` 管理的定时器 |
| Boss 星生成 | `BossBattleAdapter` | 清除星生成定时器 |
| 塔计时器 | `TowerSystem` | `clearInterval` 战斗计时器 |

### 战斗 UI 渲染阻塞 (GameBattleRenderer.js)

`_paused` 为 true 时跳过的更新：

| 行号 | 跳过的内容 |
|------|-----------|
| 314 | 暂停按钮以外的战斗 UI |
| 703 | 技能效果更新 |
| 770 | 连击动画 |
| 1025 | 下落星星渲染 |
| 1030 | 偷星者更新 |
| 1093 | 毒池更新 |

---

## 五、恢复路径

```
PAUSED 状态界面 (game.js:4120-4185):
  ├─ 继续按钮
  │   ├─ resumeAnimations()
  │   ├─ Boss  → BossBattleAdapter.togglePause()
  │   ├─ 塔   → ModeLifecycleManager.resumeActive()
  │   ├─ 闯关 → StageModeSystem.togglePause()
  │   └─ 其他 → GameLifecycleSystem.resumeGame()
  │
  ├─ 重新开始按钮
  │   ├─ resumeAnimations()
  │   └─ 各模式重新初始化
  │
  └─ 退出按钮 → WORLDMAP

TOWER_RESUME 状态 (TowerRenderer.js:724-780):
  ├─ 继续 → towerSystem.init() → transitionTo(TOWER)
  └─ 放弃 → 清除进度

广告关闭 → onClose → resumeGame()
```

---

## 六、合法状态转移

`StateMachine.js` 中定义的 PAUSED 相关转移：

```
playing ──→ paused
paused ──→ playing | menu | boss_battle | tower_combat | stage_playing | season_playing

boss_battle ──→ paused
tower_combat ──→ paused
stage_playing ──→ paused
season_playing ──→ paused
```

---

## 七、配置维度的暂停行为

`CombatSpec.js` 中定义：

```js
// 行 117: 普通模式
pauseBehavior: 'freeze'

// 行 126, 161, 171: 所有战斗模式
pauseButton: true   // 都显示暂停按钮
```

Boss / 塔 / 闯关没有覆盖 `pauseBehavior`，默认继承 `'freeze'`。

### 各时长常量（会被暂停影响或需要补偿的）

| 常量 | 值 | 所属系统 | 文件 |
|------|-----|---------|------|
| `MONSTER.ATTACK_INTERVAL_MS` | 2000ms | 怪物攻击 | `CombatSpec.js` |
| `COMBO.STAR_DURATION_MS` | 5000ms | 连击星 | `CombatSpec.js` |
| `STATUS.POISON_DURATION_MS` | 3000ms | 中毒 | `CombatSpec.js` |
| `STATUS.POISON_TICK_MS` | 1000ms | 毒伤频率 | `CombatSpec.js` |
| `SPECIAL_STARS.DODGE_DURATION_MS` | 1000ms | 闪避 | `CombatSpec.js` |
| `BOSS_STAR_DURATION` | 5000ms | Boss星 | `BossStarSystem.js` |
| `BOSS_STUN_DURATION` | 1000ms | Boss眩晕 | `BossStarSystem.js` |
| `BREAK_DURATION` | 5000ms | 偷星破防 | `StarThiefSystem.js` |
| 毒池 duration | 8000ms | 毒池 | `PoisonPuddleSystem.js` |
| 存档防抖 | 2000ms | 存档 | `StorageSystem.js` |

---

## 八、架构问题与潜在风险

### 1. 补偿不统一

- `BattleEngine.resume()` 补偿了 8+ 个时间状态，是补偿最全的地方。
- `BossBattleAdapter` 和 `GameLifecycleSystem` 各自做了一遍星星补偿和连击补偿，存在重复逻辑。
- `StarThiefSystem` 的补偿是独立的，但只在 `GameLifecycleSystem.resumeGame()` 中通过 `resumeThief()` 触发补偿。如果通过其他路径恢复（如 `ModeLifecycleManager`），偷星者补偿可能被遗漏。

### 2. 模式生命周期管理器的"薄层"问题

`ModeLifecycleManager` 只处理模式级定时器的创建/清理，但暂停/恢复时底层各系统（BattleEngine、AnimationSystem、StarThiefSystem）仍需要各自维护自己的暂停状态。没有一个统一的"暂停上下文"对象可以一次性传递 pauseDuration。

### 3. 动画系统与其他系统的时钟脱钩

`AnimationSystem` 用的是自己的虚拟时钟（`_pauseAccumulated`），而 `BattleEngine` 等用的是 `pauseStartTime + Date.now()` 计算。两者在暂停时是独立计时的，理论上应该一致，但没有交叉校验。

### 4. 塔模式的持久化暂停 vs 瞬时暂停

塔模式有两层暂停：
- **瞬时暂停**（`pauseCombat()` / `resumeCombat()`）：战斗中暂停，不清除进度。
- **持久化暂停**（`pauseTower()`）：退出时将怪物 HP 和网格状态序列化到 `playerData`，下次启动时从 `TOWER_RESUME` 恢复。

这两层之间没有互斥保护。

### 5. 状态机转移缺少 `PAUSED → TOWER_RESUME`

`StateMachine.js` 中 `paused` 可以转移到 `tower_combat` 但不能到 `tower_resume`。如果塔战斗暂停后用户点了"退出"，实际走的是 `PAUSED → menu`，然后从菜单重新进入 `TOWER_RESUME`。这条路径是非直接的两步转移。

### 6. 渲染层 `_paused` 的双来源

`GameBattleRenderer._paused` 同时聚合 `state===PAUSED` 和 `_bossPaused`，意味着 Boss 模式的暂停可能由两个独立控制源驱动，存在不一致风险。

---

## 九、改进建议

1. **统一暂停上下文对象**：创建 `PauseContext { startTime, duration }` 贯穿所有 resume 调用，避免各系统各自计算 `Date.now() - pauseStartTime`。
2. **补偿逻辑集中在 BattleEngine**：`BossBattleAdapter` 和 `GameLifecycleSystem` 中的星星/连击补偿应委托给 `BattleEngine`。
3. **动画时钟与战斗时钟统一基准**：让 `AnimationSystem.getGameTime()` 和 `BattleEngine` 的补偿计算使用同一个时间源。
4. **状态机补充 `paused → tower_resume`** 直接转移。
5. **双来源 `_paused` 合并为单一信号**。

---

## 十、落地方案

完整方案已独立为 `e:\AIGame\V1\PauseCoordinator落地方案.md`，此文档为现状分析，方案文档为唯一落地方案。