# 文案包装一致性·第八轮遗漏修正

> **产出日期**：2026-05-20
> **依据**：对第七轮修改进行全量验证 + 第九轮全频终审扫描
> **说明**：第七轮遗漏清单已全部清零。本清单仅列第八轮全频扫描发现的新遗漏。

---

## 一、"血量"残留（10处，MonsterConfig + NormalBattleAdapter）

之前修改主要针对"HP""生命值"，遗漏了"血量"一词。

### 1.1 MonsterConfig.js "血量"（9处）

| 行号 | 当前 | 改为 |
| --- | --- | --- |
| 212 | `'血量低于30%攻击+50%'` | `'灵核活性低于30%冲击+50%'` |
| 364 | `'血量低于50%攻击+50%'` | `'灵核活性低于50%冲击+50%'` |
| 398 | `'血量降至一半时会再次崩散'` | `'灵核活性降至一半时会再次崩散'` |
| 404 | `'血量50%时分裂成3只铜锈碎片'` | `'灵核活性50%时分裂成3只锈灵碎片'` |
| 444 | `'血量低于30%攻击翻倍'` | `'灵核活性低于30%冲击翻倍'` |
| 524 | `'血量50%以下攻击+80%'` | `'灵核活性50%以下冲击+80%'` |
| 547 | `'血量30%以下攻击翻倍'` | `'灵核活性30%以下冲击翻倍'` |
| 569 | `'血量30%以下攻击+150%'` | `'灵核活性30%以下冲击+150%'` |
| 664 | `'血量50%时分裂成3只铜锈碎片'` | `'灵核活性50%时分裂成3只锈灵碎片'` |

### 1.2 NormalBattleAdapter.js "血量"（1处）

| 行号 | 当前 | 改为 |
| --- | --- | --- |
| 1804 | `addMessage('血量不足', '#ff6b6b')` | `addMessage('灵核活性不足', '#ff6b6b')` |

---

## 二、"连击"残留（13处，遍及4个文件）

之前改的是 SkillConfig/TaskSystem 中的"连击"，但 StageConfig/MonsterConfig/SquadRenderer/NormalBattleAdapter 中的"连击"描述一直未被覆盖。世界观战斗术语应为"连灵"。

### 2.1 StageConfig.js "达成XX连击"（4处）

| 行号 | 当前 | 改为 |
| --- | --- | --- |
| 25 | `'学习灵光连击，达成10连击'` | `'学习连灵技巧，达成10连灵'` |
| 29 | `'达成10连击'` | `'达成10连灵'` |
| 93 | `'达成30连击'` | `'达成30连灵'` |
| 141 | `'达成50连击'` | `'达成50连灵'` |

### 2.2 MonsterConfig.js 怪物技能"连击"（3处）

| 行号 | 当前 | 改为 |
| --- | --- | --- |
| 139 | `'15%概率连击'` | `'15%概率连灵'` |
| 325 | `'25%概率连击'` | `'25%概率连灵'` |
| 483 | `'30%连击'` | `'30%连灵'` |

### 2.3 NormalBattleAdapter.js tip 提示（1处）

| 行号 | 当前 | 改为 |
| --- | --- | --- |
| 700 | `'连击加成生效中！连续点击分数翻倍'` | `'连灵加成生效中！连续触碰灵辉翻倍'` |

### 2.4 MonsterSkillSystem.js/NormalBattleAdapter.js 动画文案（2处）

| 文件 | 行号 | 当前 | 改为 |
| --- | --- | --- | --- |
| `MonsterSkillSystem.js` | 132 | `'连击!'` | `'连灵!'` |
| `NormalBattleAdapter.js` | 1422 | `'连击!'` | `'连灵!'` |

### 2.5 SquadRenderer.js 属性面板（3处）

| 行号 | 当前 | 改为 |
| --- | --- | --- |
| 493 | `fillText('【连击属性】', ...)` | `fillText('【连灵属性】', ...)` |
| 497 | `{ name: '连击阈值', ... }` | `{ name: '连灵阈值', ... }` |
| 500 | `{ name: '连击时限', ... }` | `{ name: '连灵时限', ... }` |

---

## 三、FaithSystem.js "连灵"与"连击"矛盾（1处）

| 行号 | 当前 | 改为 |
| --- | --- | --- |
| 54 | `name: '连灵宗师', description: '连击中断时间延长1秒'` | `name: '连灵宗师', description: '连灵中断时间延长1秒'` |

---

## 四、"分数"残留（1处）

| 文件 | 行号 | 当前 | 改为 |
| --- | --- | --- | --- |
| `SquadRenderer.js` | 499 | `{ name: '分数加成', ... }` | `{ name: '灵辉加成', ... }` |

---

## 五、"怪物"残留（1处）

| 文件 | 行号 | 当前 | 改为 |
| --- | --- | --- | --- |
| `NormalBattleAdapter.js` | 494 | `addMessage('💨 怪物觉醒闪避!', '#ff6b6b')` | `addMessage('💨 邪灵觉醒闪避!', '#ff6b6b')` |

---

## 六、MonsterConfig 怪物技能纯数值公式（5处，可选改）

这些是 rage/armor 技能的 description，纯数值无叙事包装。建议添加灵域叙事前缀。

| 行号 | 当前 | 建议改为 |
| --- | --- | --- |
| 230 | `'35%闪避攻击'` | `'灵核虚化：35%闪避灵光冲击'` |
| 231 | `'反弹20%冲击'` | `'灵能反射：反弹20%灵光冲击'` |
| 365 | `'减伤20%'` | `'灵场强化：减轻20%灵光冲击'` |
| 525 | `'反弹40%冲击'` | `'灵能反射：反弹40%灵光冲击'` |
| 526 | `'减伤30%'` | `'灵场强化：减轻30%灵光冲击'` |

---

## 七、汇总

| 类别 | 数量 | 涉及文件 |
| --- | --- | --- |
| "血量"残留 | 10 | MonsterConfig/NormalBattleAdapter |
| "连击"残留 | 13 | StageConfig/MonsterConfig/NormalBattleAdapter/MonsterSkillSystem/SquadRenderer |
| FaithSystem "连灵"矛盾 | 1 | FaithSystem |
| "分数"残留 | 1 | SquadRenderer |
| "怪物"残留 | 1 | NormalBattleAdapter |
| 纯数值公式（可选改） | 5 | MonsterConfig |
| **合计** | **31** | — |