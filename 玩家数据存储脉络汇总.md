# 玩家数据存储脉络汇总

> 分析范围：`e:\AIGame\V1\`，涵盖玩家游戏数据（playerData）的存储架构、数据流、版本迁移与完整结构。

---

## 一、存储架构总览

底层存储引擎为浏览器 `localStorage`，通过统一 API 层封装。数据流向：

```
各游戏系统调用 savePlayerData() 触发保存
        │
        ▼
StorageSystem.js (防抖写，延迟 2000ms 批量合并)
        │
        ▼
BrowserAPI.js → localStorage.setItem('game_playerData', JSON.stringify(data))
```

读取时为反向流程：

```
localStorage.getItem('game_playerData')
        │
        ▼
BrowserAPI.getStorageSync() → JSON.parse → 原始数据
        │
        ▼
PlayerDataSystem.loadPlayerData() → 迁移/合并/修复 → 写入 playerData
```

> **关键特性**：多个子系统可能在同帧内多次调用 `savePlayerData()`，StorageSystem 通过防抖将 2000ms 内的多次调用合并为一次实际写入，避免频繁 I/O。

---

## 二、关键文件一览

| 文件 | 角色 |
|------|------|
| `V1/src/platform/BrowserAPI.js` L161-189 | 底层 localStorage 读写，所有 key 加 `game_` 前缀 |
| `V1/src/systems/StorageSystem.js` L1-217 | 存档系统核心：防抖保存、最高分管理、数据清除、云上传 |
| `V1/src/systems/PlayerDataSystem.js` L1-342 | 数据加载入口、默认模板定义、版本迁移（v0→v2） |
| `V1/game.js` L740-810, L988-1001 | 全局 `playerData` 声明 + 向各子系统注入 `savePlayerData` 包装函数 |
| `V1/src/utils/DeepClone.js` | 递归深拷贝工具，保存/加载时使用，避免引用污染 localStorage |
| `V1/src/systems/ModeLifecycleManager.js` L222-223 | 模式退出时自动触发 saveData |
| `V1/src/systems/worldmap/WorldMapExploration.js` L68-82 | 世界地图进度 JSON 序列化保存，`immediate=true` 立即写入 |

---

## 三、存储的 Key 清单

所有数据存储在 `localStorage` 中，前缀统一为 `game_`：

| localStorage Key | 存储内容 | 格式 |
|-------------------|---------|------|
| `game_playerData` | 完整玩家存档（20+ 顶层字段） | JSON 字符串 |
| `game_bestScore` | 历史最高分 | 数字 |
| `game_cloudData` | 排行榜云端同步数据 | JSON 字符串 |
| `game_ui_layout_overrides` | UI 布局覆盖配置 | JSON `{ version, elements }` |

---

## 四、底层 API 实现 (`BrowserAPI.js`)

```161:189:V1/src/platform/BrowserAPI.js
getStorageSync(key) {
    // 实际 key = 'game_' + key
    // localStorage.getItem → JSON.parse
    // 失败返回 ''
},
setStorageSync(key, val) {
    // JSON.stringify(val) → localStorage.setItem('game_' + key, val)
},
removeStorageSync(key) {
    // localStorage.removeItem('game_' + key)
}
```

---

## 五、存档管理 (`StorageSystem.js`)

核心流程：

```
savePlayerData(immediate=false)
  │
  ├─ immediate=true  → doSavePlayerData() 直接写入
  │
  └─ immediate=false → 启动 2000ms 防抖定时器
                       多次调用重置定时器
                       timeout 触发 → doSavePlayerData()

doSavePlayerData()
  │
  ├─ deepClone(playerData)   ← 避免引用污染
  └─ setStorageSync('playerData', dataToSave)
```

其他功能入口：

| 方法 | 功能 |
|------|------|
| `loadBestScore()` | 读取 `game_bestScore` |
| `saveBestScore()` | 新分 > 旧分时写入 |
| `clearGameData()` | 删除 `game_playerData` + `game_bestScore`，重置为精简默认值 |
| `uploadLeaderboardData()` | 上传 best_score + total_kills + season_score 到云端 |
| `uploadSeasonScore()` | 上传赛季分数 |

---

## 六、数据加载流程 (`PlayerDataSystem.js`)

```169:323:V1/src/platform/BrowserAPI.js
// 实际在 PlayerDataSystem.js

loadPlayerData()
  │
  ├─ 1. getStorage('playerData') → 原始 JSON
  │
  ├─ 2. 以 defaultPlayerData 为骨架模板
  │    逐个字段用保存数据覆盖（缺失字段保留默认值）
  │
  ├─ 3. 旧数据兼容修复：
  │   ├─ 去重 ownedCharacters
  │   ├─ 技能格式迁移：字符串 → { uid, id, level }
  │   └─ characterExperience 类型修复
  │
  ├─ 4. 版本迁移（v0 → v1 → v2）：
  │   └─ 增加 upgradeData.stars 嵌套字段
  │
  ├─ 5. 恢复 timeCrystalUnlocked 状态
  │
  └─ 6. 补全 infiniteTower 缺失字段
```

**版本迁移配置**：

```
CURRENT_SAVE_VERSION = 2

MIGRATIONS[0] (v0 → v1): 占位
MIGRATIONS[1] (v1 → v2): 增加 upgradeData.stars 嵌套字段
```

迁移在 `loadPlayerData()` 中自动执行，完成后立即 `savePlayerData(true)` 持久化。

---

## 七、playerData 完整数据结构 (v2)

### 顶层字段

| 字段 | 类型 | 说明 |
|------|------|------|
| `_version` | `number` | 存档版本号，当前 = 2 |
| `id` | `string` | 玩家 ID |
| `name` | `string` | 玩家名称 |
| `gold` | `number` | 灵币 |
| `starSource` | `number` | 器灵石 |
| `starStones` | `number` | 星石 |
| `ownedCharacters` | `string[]` | 拥有的角色 ID 列表 |
| `currentCharacterId` | `string/null` | 当前出战角色 ID |
| `characterExperience` | `{ [charId]: { level, exp, maxExp } }` | 角色经验 |

### 材料背包 (`materials`)

每种材料含 `quantity`（当前数量）和 `usedCount`（已使用次数）：

| 材料字段 | 说明 |
|----------|------|
| `iceCrystal` | 冰晶 |
| `fireSource` | 火源 |
| `critCrystal` | 暴击结晶 |
| `critFireSource` | 暴击火源 |
| `timeCrystal` | 时之结晶 |
| `devourerResidue` | 吞噬者残渣 |

### 道具背包 (`items`)

| 道具字段 | 说明 |
|----------|------|
| `healPotion` | 治疗药水 |
| `timePotion` | 时间药水 |
| `expPotionSmall` | 小经验药水 |
| `expPotionMedium` | 中经验药水 |
| `expPotionLarge` | 大经验药水 |

### 装备 (`equipments`)

```
owned: [{ uid, id, rarity, ... }]
equipped: { weapon, armor, accessory, set }  // 槽位 → 装备uid
```

### 技能 (`skills`)

```
owned: [{ uid, id, level }]    // v2 格式：含 uid 和 level
equipped: []                    // 装备的技能 uid 列表
gachaTickets: number            // 抽卡券
```

### 宠物 (`pets`)

```
owned: []         // 拥有的宠物列表
equipped: null    // 当前装备的宠物
```

### 灵韵系统

| 字段 | 说明 |
|------|------|
| `unlockedStarTypes: []` | 已解锁灵韵类型 |
| `equippedStars: []` | 已装备灵韵 |
| `normalStarEnabled: true` | 普通星启用标志 |
| `starMode: 'random' \| 'falling'` | 星模式 |

### 灵韵等级

| 字段 | 说明 |
|------|------|
| `iceStarLevel: 0` / `maxIceStarLevel: 10` | 冰星等级 |
| `fireStarLevel: 0` / `maxFireStarLevel: 10` | 火星等级 |
| `timeStarLevel: 0` / `maxTimeStarLevel: 10` | 时星等级 |
| `timeCrystalUnlocked: false` | 时晶解锁标志 |

### 玩家属性

| 字段 | 说明 |
|------|------|
| `playerHp: 100` / `maxPlayerHp: 100` | 玩家血量 |
| `extraCritRate: 0` / `extraCritDamage: 0` | 额外暴击率/暴伤 |
| `totalMonstersKilled: 0` | 总击杀数 |
| `bossKillCount: 0` / `firstBossKilled: false` | Boss 击杀统计 |

### 任务进度 (`taskProgress`)

```
guide: {}           // 引导任务进度
daily: {}           // 每日任务进度
achievements: {}    // 成就进度
lastDailyRefresh: 0 // 上次每日刷新时间戳
stats: {
    totalClicks, totalGames, totalPerfects, ...
}
```

### 关卡进度 (`stageProgress`)

```
{}
```

### 信仰数据 (`faithData`)

```
resources: {
    devoutMark, divineEssence, originCrystal,
    weeklyDevoutMark, lastWeeklyReset
}
characters: {}
inheritance: {
    totalInherited,
    inheritedBonus: { attack, hp, critRate }
}
```

### 无尽之塔 (`infiniteTower`)

```
highestFloor: 0      // 最高层
totalClears: 0       // 总通关次数
currentFloor: 1      // 当前层
currentHp: 100       // 当前血量
maxHp: 100           // 最大血量
collectedRewards: [] // 已收集奖励
exploredCells: []    // 已探索格子
playerX: 0           // 玩家 X 坐标
playerY: 0           // 玩家 Y 坐标
totalKills: 0        // 总击杀
totalTreasures: 0    // 总宝箱
totalMaterials: 0    // 总材料
isPaused: false      // 是否暂停
grid: null           // 网格数据
blindSteps: 0        // 盲步数
```

### 挂机数据 (`afkData`)

```
lastClaimTime: 0             // 上次领取时间戳
maxOfflineHours: 8           // 最大离线小时
baseGoldPerMinute: 10        // 每分钟灵币基础收益
baseExpPerMinute: 2          // 每分钟经验基础收益
accumulatedRewards: {        // 累积未领取奖励
    gold, exp, materials, equipments
}
accumulatedHours: 0          // 累积离线小时
```

### 升星数据 (`upgradeData`) — v2 新增

```
stars: {}
```

---

## 八、运行时额外字段

以下字段在 `game.js` 中直接在 `playerData` 上声明，不在 `defaultPlayerData` 模板中，属于惰性初始化或上下文注入：

| 字段 | 说明 |
|------|------|
| `worldMapProgress` | 世界地图进度：`{ currentWorldId, worlds: { [id]: { playerPos, entities } } }` |
| `usedMaterials` | 已使用材料记录 |
| `seenTips` | 已查看提示 |
| `monthlyCards` | 月卡数据 |
| `godMode` | 无敌模式标志 |
| `playerShield` | 玩家护盾 |
| `playerRage` | 玩家怒气 |
| `tutorialCompleted` | 教程是否完成 |

---

## 九、所有数据写入触发点

以下是调用 `savePlayerData()` 的所有系统（约 20+ 处），按 `game.js` 注入位置列出：

```
game.js 初始化时注入 saveData 包装函数到各子系统：

偷灵系统    → game.js L1149
收服系统    → game.js L1166
任务系统    → game.js L1175
信仰系统    → game.js L1192
爬塔系统    → game.js L1336
挂机系统    → game.js L1353
抽卡系统    → game.js L1383
角色系统    → game.js L1407
掉落系统    → game.js L1431
材料系统    → game.js L1682
月卡系统    → game.js L1730
Boss战系统  → game.js L1997/L2011/L2042
普通战斗    → game.js L2494/L2522/L2563/L2614-2615
世界地图    → game.js L2950 (+ WorldMapExploration.js L81)
模式生命周期 → game.js L3011-3012
调试系统    → game.js L3148
排行榜      → game.js L3298/L3309
动画系统    → game.js L3221
赛季系统    → game.js L3360
小队渲染    → game.js L4641/L4661/L4909
背包渲染    → game.js L6085
```

---

## 十、数据流向图

```
              ┌──────────────┐
              │  playerData  │  ← 全局可变对象
              │  (game.js)   │
              └──────┬───────┘
                     │
      ┌──────────────┼──────────────┐
      │ 各子系统通过注入的          │  模式退出自动保存
      │ saveData() 触发保存         │  (ModeLifecycleManager)
      └──────────────┼──────────────┘
                     │
                     ▼
          ┌──────────────────┐
          │  StorageSystem   │
          │  savePlayerData  │
          │  (防抖 2000ms)   │
          └────────┬─────────┘
                   │
          ┌────────┴─────────┐
          │   deepClone()    │
          │   深拷贝快照      │
          └────────┬─────────┘
                   │
          ┌────────┴─────────┐
          │   BrowserAPI     │
          │   setStorageSync │
          └────────┬─────────┘
                   │
          ┌────────┴─────────┐
          │   localStorage   │
          │  game_playerData │
          └──────────────────┘

          加载流程（反向）：

          ┌──────────────────┐
          │   localStorage   │
          │  game_playerData │
          └────────┬─────────┘
                   │
          ┌────────┴─────────┐
          │   BrowserAPI     │
          │   getStorageSync │
          │   → JSON.parse   │
          └────────┬─────────┘
                   │
          ┌────────┴─────────┐
          │ PlayerDataSystem │
          │  loadPlayerData  │
          │  → 模板合并      │
          │  → 旧数据修复    │
          │  → 版本迁移      │
          │  → 字段补全      │
          └────────┬─────────┘
                   │
                   ▼
          ┌──────────────────┐
          │   playerData     │ ← 游戏使用
          └──────────────────┘
```

---

## 十一、关键设计决策

1. **防抖合并写入**：多个子系统可能在同帧内多次触发保存，2000ms 防抖将其合并为单次 `localStorage` 写入。
2. **骨架模板合并**：加载时以 `defaultPlayerData` 为模板，缺失字段自动回填默认值，保证新版本新增字段对旧存档透明兼容。
3. **版本迁移链**：`_version` 字段驱动增量迁移，从 v0 到当前版本逐级执行，每次迁移后立即持久化。
4. **深拷贝快照**：保存时 `deepClone(playerData)` 切断引用，防止后续运行时修改污染已存储数据。
5. **即时保存通道**：`savePlayerData(true)` 跳过防抖直接写入，用于世界地图等需要即时持久化的场景。